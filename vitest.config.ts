import { defineConfig } from "vitest/config";

// 유닛 테스트 전용 설정. (vite.config.ts의 React/PWA 플러그인을 로드하지 않도록 별도 파일)
export default defineConfig({
  test: {
    environment: "node",
    // 루트의 *.test.ts도 포함한다 — middleware.ts(운영 사이트 인증 관문)가 src 밖에 있어
    // 종전에는 유닛의 사정권 밖이었다. 커버리지 include는 그대로라 임계값에는 영향이 없다.
    include: ["src/**/*.test.ts", "*.test.ts"],
    coverage: {
      provider: "v8",
      reporter: ["text", "html", "lcov"],
      // 측정 대상: store · utils · **hooks**.
      //
      // hooks를 넣는 이유: 종전에는 store+utils만 재서 소스의 46%만 지표가 있었고,
      // 나머지 53%(hooks·components·app)는 숫자조차 없었다. 그 사각지대에서 결함이
      // 반복해서 나왔다 — 실제로 한 번의 코드 리뷰에서 찾은 6건 중 4건이 여기 살았고,
      // 그중 둘(퀵의 죽은 오답 키 읽기, 전멸형 Promise.all)이 hooks/였다.
      // 커버리지가 낮은 것과 커버리지를 모르는 것은 다르다. 낮은 채로 두더라도
      // **보이게** 두어야 나빠지는 것을 알 수 있다.
      //
      // components/app은 아직 넣지 않는다. 3,499줄이 사실상 0%라 넣으면 전체 수치가
      // 반토막 나면서 임계값이 무의미해지고, 그것들은 뷰 계층이라 E2E가 맞는 도구다.
      // hooks는 다르다 — 순수 로직을 품고 있어 밖으로 꺼내면 유닛으로 잡을 수 있다
      // (reviewTargetIds가 그 예다: effect 안에 묻혀 있던 계산을 꺼내 검사로 고정했다).
      include: ["src/store/**", "src/utils/**", "src/hooks/**"],
      // 임계값은 "지금보다 나빠지지 않는다"는 바닥이다(실측보다 약 2%p 낮게).
      //
      // 종전(79/70/77/81)보다 낮아 보이는 것은 테스트가 줄어서가 아니라 **재는 범위가
      // 넓어졌기 때문이다.** hooks를 포함하면서 분모가 늘었다.
      //
      // 이 값을 올리는 방법은 렌더러를 들이는 것이 아니라, 훅 안의 순수 로직을 모듈로
      // 꺼내 유닛으로 덮는 것이다 — reviewTargetIds(useQuestions), roundHistory
      // (useQuizSession의 회차 조립)가 그 사례다. 꺼낼 때마다 여기 임계값도 함께 올린다.
      // hooks 디렉터리 자체의 %는 크게 오르지 않는다(남는 건 React 글루다) — 오르는 건
      // 전체 수치이고, 그게 이 래칫이 재려는 값이다.
      //
      // 래칫 기록
      //   2026-08-07 (1) 72.51 / 66.98 / 67.13 / 74.72 → 70 / 65 / 65 / 72  (hooks 편입)
      //   2026-08-07 (2) 75.28 / 71.06 / 70.42 / 77.30 → 73 / 69 / 68 / 75  (roundHistory 추출)
      //   2026-08-07 (3) 77.80 / 72.64 / 76.11 / 79.60 → 75 / 71 / 74 / 77  (오류·엣지 경로)
      //   2026-08-13 (1) 78.30 / 72.58 / 75.99 / 80.04 → 76 / 71 / 74 / 78  (quickStats 유닛 신설)
      //     quickStats.ts는 0%였다 — 측정 대상에 들어 있는데 값이 0이라 '낮은 것'이 아니라
      //     아무도 안 재는 것이었고, 실제로 그 안에서 결함이 두 번 나왔다(퀵 확정 규칙,
      //     점수판 되감김). 이때는 브랜치·함수 실측이 기존 임계값과 2%p 안쪽이라 두 값을
      //     그대로 뒀다 — 래칫은 내리지 않는다.
      //   2026-08-13 (2) 80.52 / 77.05 / 77.14 / 81.85 → 78 / 75 / 75 / 79  (브랜치 집중 보강)
      //     '처음 닿게 하는' 단계가 대체로 끝나서, 이번에는 **분기별 경계**를 덮었다:
      //     손상·구버전 데이터(정제기의 프로토타입 오염 키·모순 수치·결측 필드),
      //     동률 정렬의 결정성, 렌더러의 비정형 블록 강등, 화면 콘솔의 문자열화·전역
      //     오류 포착, 스크롤 잠금의 중첩·이중 해제, 하드웨어 뒤로가기의 웹/네이티브 분기.
      //     남은 최대 덩어리는 hooks(브랜치 151개, 11.7%)다 — 렌더러 없이는 닿지 않으므로
      //     다음 차례는 훅 안의 순수 로직을 모듈로 꺼내는 쪽이다(위 문단의 방법).
      thresholds: {
        statements: 78,
        branches: 75,
        functions: 75,
        lines: 79,
      },
    },
  },
});
