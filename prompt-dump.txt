"<USER_REQUEST>\n/goal docs 에 있는 codex-fix-issues-phase4-review.md 를 보고 현재 코드를 수정해주는데\n\n시작 전 너가 보기에 중요한 UI, 코드, 데이터 정합성, 최적화 방면에서 각각 10가지씩 만족하는 규칙을 세워서 모두 완료하고\n\n테스트 검즘까지 ALL PASS 가 나야해\n</USER_REQUEST>\n<ADDITIONAL_METADATA>\nThe current local time is: 2026-06-14T13:48:34+09:00.\n\nThe user has mentioned some items in the form @[ITEM]. Here is extra information about the items that were mentioned by the user, in the order that they appear:\n\n/goal is a [Slash Command]:\nThe user has marked this task with /goal, indicating that this task is intended to run for a long time without user input, e.g. overnight. You should be extra thorough and only stop when you are confident the goal has been completely fulfilled. The system will force you to continue execution, prompting you to audit your work until completion.\n</ADDITIONAL_METADATA>\n<USER_SETTINGS_CHANGE>\nThe user changed setting `Model Selection` from None to Gemini 3.1 Pro (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.\n</USER_SETTINGS_CHANGE>"

"<USER_REQUEST>\n/goal docs 에 있는 codex-fix-issues-phase4-review.md 를 보고 현재 코드를 수정해주는데\n\n시작 전 너가 보기에 중요한 UI, 코드, 데이터 정합성, 최적화 방면에서 각각 10가지씩 만족하는 규칙을 세워서 모두 완료하고\n\n테스트 검즘까지 ALL PASS 가 나야해\n</USER_REQUEST>\n<ADDITIONAL_METADATA>\nThe current local time is: 2026-06-14T13:48:34+09:00.\n\nThe user has mentioned some items in the form @[ITEM]. Here is extra information about the items that were mentioned by the user, in the order that they appear:\n\n/goal is a [Slash Command]:\nThe user has marked this task with /goal, indicating that this task is intended to run for a long time without user input, e.g. overnight. You should be extra thorough and only stop when you are confident the goal has been completely fulfilled. The system will force you to continue execution, prompting you to audit your work until completion.\n</ADDITIONAL_METADATA>\n<USER_SETTINGS_CHANGE>\nThe user changed setting `Model Selection` from None to Gemini 3.1 Pro (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.\n</USER_SETTINGS_CHANGE>"

"<USER_REQUEST>\n/goal 시작 전 너가 보기에 중요한 UI, 코드, 데이터 정합성, 최적화 방면에서 각각 10가지씩 만족하는 규칙을 세워서 모두 완료하고\r\n\r\n테스트 검즘까지 ALL PASS 가 나야해\n\n아래의 리뷰를 보고 진행해\n\n# `codex/fix-issues-in-phase-4` 브랜치 코드 리뷰 리포트\r\n\r\n대상 저장소: `WindowHyun/ISTQB`\r\n대상 브랜치: `codex/fix-issues-in-phase-4`\r\n비교 기준: `main`\r\n대상 파일: `www/script.js`, `www/style.css`, `www/index.html`\r\n\r\n---\r\n\r\n## 0. 요약\r\n\r\n| 등급 | 개수 | 설명 |\r\n|---|---|---|\r\n| 🔴 Critical | 5건 | 핵심 기능이 동작하지 않거나 데이터가 손상될 수 있는 버그 |\r\n| 🟠 High | 4건 | 특정 조건에서 화면/동작이 명백히 잘못되는 문제 |\r\n| 🟡 Medium | 6건 | UX 저하, 접근성/다크모드 누락, 잠재적 회귀 |\r\n| ⚪ Low | 3건 | 코드 정리, 유지보수성 |\r\n\r\n가장 심각한 항목은 **#1 오답 노트 기능 자체가 동작하지 않는 버그**입니다. 이번 브랜치의 핵심 신규 기능(README에 명시된 \"오답 다시풀기\" / \"오답 노트\")이 채점 결과를 전혀 반영하지 못합니다.\r\n\r\n---\r\n\r\n## 1. 🔴 [Critical] 오답 노트 기능이 항상 \"전부 오답\"으로 표시됨\r\n\r\n### 위치\r\n`www/script.js` — 채점(`gradeBtn` 클릭) 핸들러 내부, `historyAnswers` 생성 부분.\r\n\r\n### 코드\r\n\r\n```js\r\n// 채점 시 히스토리에 저장할 답안을 추출하는 부분\r\nconst historyAnswers = {};\r\nObject.keys(state.answers).forEach(key => {\r\n  if (key.includes(`-${targetMode}-`)) {\r\n     historyAnswers[key] = state.answers[key];\r\n  }\r\n});\r\n\r\nstate.histories.push({\r\n  id: timestamp.toString(),\r\n  timestamp,\r\n  mode: state.mode,\r\n  setId: targetSetId,\r\n  answers: historyAnswers,\r\n  randomRefs: state.mode === \"random\" ? [...state.randomRefs] : null\r\n});\r\n```\r\n\r\n### 문제\r\n\r\n이번 브랜치에서 `answerKey()`가 다음과 같이 변경되었습니다 (Phase 4의 핵심 개선 중 하나):\r\n\r\n```js\r\nfunction answerKey(question, mode = answerMode()) {\r\n  if (question.id) return `${question.id}-${mode}`;   // 신규 포맷: \"ISTQB-FL-V4-A-001-exam\"\r\n  return legacyAnswerKey(question, mode);\r\n}\r\n\r\nfunction legacyAnswerKey(question, mode = answerMode()) {\r\n  const setId = question.legacySetId || question.setId || state.setId;\r\n  return `${setId}-${mode}-${question.number}`;       // 구 포맷: \"ISTQB-FL-V4-A-exam-1\"\r\n}\r\n```\r\n\r\n실제 데이터(`www/data/istqb/sample-a.json` 등)의 모든 문제에는 `id` 필드가 존재합니다 (`\"id\": \"ISTQB-FL-V4-A-001\"`). 따라서 `chooseOption()` → `saveAnswers()`로 저장되는 실제 키는 항상 **신규 포맷** `\"ISTQB-FL-V4-A-001-exam\"` 형태입니다.\r\n\r\n그런데 히스토리 생성 시 필터는 `key.includes(`-${targetMode}-`)`, 즉 **`\"-exam-\"`(앞뒤로 하이픈)이 포함되어야** 함을 요구합니다.\r\n\r\n```text\r\n신규 포맷: \"ISTQB-FL-V4-A-001-exam\"  → \"-exam-\" 포함?  ❌ (끝이 \"-exam\"으로 끝나서 뒤에 \"-\"가 없음)\r\n구 포맷:   \"ISTQB-FL-V4-A-exam-1\"    → \"-exam-\" 포함?  ✅\r\n```\r\n\r\n결과적으로 **`historyAnswers`는 항상 빈 객체 `{}`** 가 됩니다.\r\n\r\n이후 오답 노트를 그릴 때:\r\n\r\n```js\r\nfunction historyWrongNoteItems(history) {\r\n  ...\r\n  questions.forEach((question) => {\r\n     const ansKey = `${question.setId || history.setId}-${history.mode}-${question.number}`; // 구 포맷 키로 조회\r\n     const selected = history.answers[ansKey] || [];   // 항상 [] (history.answers === {})\r\n     const isCor =\r\n       question.type === \"short_answer\"\r\n         ? normalizeTextAnswer(selected[0]) === normalizeTextAnswer(question.answer[0])\r\n         : sameChoices(selected, question.answer);      // sameChoices([], [...]) → 항상 false\r\n\r\n     if (!isCor) {\r\n       items.push({ ... });   // 모든 문제가 여기로 들어감\r\n     }\r\n  });\r\n}\r\n```\r\n\r\n`selected`가 항상 빈 배열이므로 `isCor`는 모든 문제에 대해 `false`가 되고, **채점 결과와 무관하게 세트 내 모든 문제(예: 40문항)가 오답 노트에 표시**됩니다.\r\n\r\n### 영향\r\n- README에서 강조한 핵심 기능(\"오답 다시풀기를 누르기 전까지 기존 오답 기록 보호\", \"오답 노트\")이 사실상 무의미해짐.\r\n- `오답 다시풀기`(`retryWrongBtn`)는 `missedExamQuestions()`를 사용하므로 별도 경로이고 정상 동작하지만, **오답 노트 모달에 표시되는 목록과 실제 채점 결과가 일치하지 않음** → 사용자에게 혼란을 줌.\r\n\r\n### 수정 방향 (제안)\r\n`historyAnswers` 필터와 `historyWrongNoteItems`의 조회 키를 모두 `answerKey(question, mode)` 기준(신규 포맷)으로 통일해야 합니다.\r\n\r\n```js\r\n// 필터: question 객체 기준으로 직접 키를 계산\r\nconst targetQuestions =\r\n  state.mode === \"random\" ? randomQuestions() : currentSet().questions;\r\n\r\nconst historyAnswers = {};\r\ntargetQuestions.forEach((question) => {\r\n  const key = answerKey(question, targetMode);\r\n  if (state.answers[key]) historyAnswers[key] = state.answers[key];\r\n});\r\n```\r\n\r\n```js\r\n// historyWrongNoteItems 조회도 동일하게\r\nconst ansKey = answerKey(question, history.mode === \"random\" ? \"random\" : \"exam\");\r\nconst selected = history.answers[ansKey] || [];\r\n```\r\n\r\n---\r\n\r\n## 2. 🔴 [Critical] `figureFor()` 키 생성 버그 — 그림이 표시되지 않음\r\n\r\n### 위치\r\n`www/script.js` `figureFor(question)`\r\n\r\n### 코드 (main vs branch)\r\n\r\n```js\r\n// main (버그)\r\nconst setId = question.setId || state.setId;\r\nconst key = `${setId}${question.number}`;   // \"ISTQB-FL-V4-A23\" → 매핑 테이블 키와 불일치\r\n\r\n// branch (수정 완료)\r\nconst sampleCode = String(setId || \"\").split(\"-\").pop();\r\nconst key = `${sampleCode}${question.number}`;  // \"A23\" → 매핑 테이블과 일치\r\n```\r\n\r\n### 상태\r\n✅ **이 브랜치에서 이미 수정됨.** (참고용으로 기록 — 회귀 방지를 위해 테스트 케이스 추가를 권장)\r\n\r\n---\r\n\r\n## 3. 🔴 [Critical] 타이머 백그라운드 복귀 시 폭증 — `elapsedSeconds` 방식으로 수정됨\r\n\r\n### 위치\r\n`www/script.js` `updateTimer()`, `state.elapsedSeconds` / `state.lastTick`\r\n\r\n### 상태\r\n✅ **이 브랜치에서 절대시각(`startedAt`) 방식 → 누적시간(`elapsedSeconds`) + `lastTick` 델타 방식으로 수정됨.**\r\n\r\n다만 아래 **#7 IndexedDB 복원 경쟁 조건**과 함께 검토 필요 (관련 상태가 비동기 복원 대상에 포함됨).\r\n\r\n---\r\n\r\n## 4. 🔴 [Critical] `randomQuestions()` — 전체 문항 수가 40개 미만이면 매 렌더마다 무한 재셔플\r\n\r\n### 위치\r\n`www/script.js`\r\n\r\n```js\r\nfunction generateRandomRefs() {\r\n  state.randomRefs = shuffle(allQuestions())\r\n    .slice(0, 40)\r\n    .map((question) => ({ setId: question.setId, number: question.number }));\r\n}\r\n\r\nfunction randomQuestions() {\r\n  if (state.randomRefs.length !== 40) generateRandomRefs();  // ⚠️\r\n  return state.randomRefs\r\n    .map((ref) => { ... })\r\n    .filter(Boolean);\r\n}\r\n```\r\n\r\n### 문제\r\n`allQuestions()`가 반환하는 전체 문항 수가 40개 미만인 제품/세트 구성이라면:\r\n\r\n- `shuffle(allQuestions()).slice(0, 40)`은 **40개보다 적은 배열**을 반환합니다.\r\n- `state.randomRefs.length !== 40` 조건이 **항상 true**가 되어,\r\n- `randomQuestions()`가 호출될 때마다(즉 `render()`가 호출될 때마다) `generateRandomRefs()`가 다시 실행됩니다.\r\n\r\n결과적으로 **랜덤 모드에 진입한 직후부터 매 렌더링마다 문제 순서가 무작위로 재섹션**됩니다. 사용자가 답을 선택해 `render()`가 다시 호출되는 순간 `currentQuestions()`(=`randomQuestions()`)의 순서/구성이 바뀌면서:\r\n- 현재 보고 있던 문제가 다른 문제로 바뀌거나,\r\n- `state.index`가 가리키는 문제가 매번 달라짐.\r\n\r\n### 현재 안전한 이유\r\n현재 ISTQB(186문항)·CSTS(각 세트 수십 문항 합산) 데이터셋은 합계가 40을 넘기 때문에 **지금 당장은 발생하지 않음**. 그러나:\r\n- 향후 세트를 줄이거나, 특정 제품에 문항이 적은 카테고리를 추가하면 즉시 재현되는 **잠재적 타임 폭탄(time bomb)**.\r\n- 전체 문항 수가 정확히 40개 이상인지 검증하는 가드가 코드 어디에도 없음.\r\n\r\n### 수정 방향 (제안)\r\n```js\r\nfunction randomQuestions() {\r\n  const targetCount = Math.min(40, allQuestions().length);\r\n  if (state.randomRefs.length !== targetCount || state.randomRefs.length === 0) {\r\n    generateRandomRefs();\r\n  }\r\n  ...\r\n}\r\n```\r\n혹은 최소한 `allQuestions().length < 40`일 때 경고 로그 + 가능한 전체 문항 수로 대체.\r\n\r\n---\r\n\r\n## 5. 🔴 [Critical] `restorePersistentSnapshot()`의 비동기 경쟁 조건 (Race Condition)\r\n\r\n### 위치\r\n`www/script.js`\r\n\r\n```js\r\n// 초기화 마지막 부분\r\nrenderExamSelect();\r\nrenderMode();\r\nrender();\r\nrestorePersistentSnapshot();   // await 하지 않음 (fire-and-forget)\r\n```\r\n\r\n```js\r\nasync function restorePersistentSnapshot() {\r\n  let snapshot = null;\r\n  let hasUiState = false;\r\n  try {\r\n    snapshot = JSON.parse(localStorage.getItem(persistenceKey()));\r\n    hasUiState = Boolean(localStorage.getItem(uiStorageKey()));   // ← 이 시점에 동기적으로 평가\r\n  } catch { snapshot = null; }\r\n\r\n  const db = await openPersistenceDb();          // ← IndexedDB open은 비동기, 수~수십ms 소요 가능\r\n  if (db) {\r\n    try {\r\n      snapshot = await new Promise((resolve) => { ... });\r\n    } catch { ... }\r\n  }\r\n  if (!snapshot || typeof snapshot !== \"object\") return;\r\n  if (snapshot.answers && Object.keys(state.answers).length === 0) {\r\n    state.answers = sanitizeAnswerState(snapshot.answers);\r\n  }\r\n  if (snapshot.uiState && !hasUiState) {\r\n    applyBackupSnapshot({ answers: state.answers, uiState: snapshot.uiState });  // ← 전체 state 덮어씀\r\n  }\r\n  renderExamSelect();\r\n  renderMode();\r\n  render();\r\n}\r\n```\r\n\r\n### 문제\r\n1. `hasUiState`는 **함수 진입 시점**(IndexedDB 열기 이전)의 `localStorage` 상태로 결정됩니다.\r\n2. 만약 사용자가 **앱 최초 진입 직후 즉시** (IndexedDB가 열리기 전, 수~수십ms 사이) 옵션을 클릭하면:\r\n   - `chooseOption()` → `saveAnswers()` + `saveUiState()` 실행 → `localStorage`에 `uiState`/`answers`가 기록됨.\r\n   - 그러나 `hasUiState` 변수는 이미 `false`로 캡처되어 있으므로, 이후 IndexedDB에서 가져온 **오래된 snapshot.uiState로 사용자의 첫 클릭 상태가 덮어써짐(`applyBackupSnapshot`)**.\r\n3. `state.answers` 조건은 `Object.keys(state.answers).length === 0`으로 체크하므로 답안 자체는 비교적 안전하지만, **`state.mode`, `state.index`, `examGraded`, `reviewRetake` 등 UI 상태 전체가 스냅샷으로 되돌아갈 수 있음** — 예: 사용자가 \"시험 모드\"로 전환했는데 다시 \"연습 모드\"로 되돌아가는 식의 깜빡임/되돌림 현상.\r\n\r\n### 영향\r\n- 실제 발생 빈도는 낮지만(타이밍 윈도우가 매우 짧음), 느린 기기나 IndexedDB 초기화가 지연되는 환경(특히 Android WebView)에서는 재현 가능성이 더 높음.\r\n- 디버깅이 매우 어려운 \"가끔 처음 클릭이 무시되는 것처럼 보이는\" 버그로 보고될 가능성.\r\n\r\n### 수정 방향 (제안)\r\n- 앱 부팅 시 `restorePersistentSnapshot()`을 **`render()` 이전에 `await`** 하거나,\r\n- `hasUiState`를 `restorePersistentSnapshot` 완료 시점에 다시 평가(재확인)하거나,\r\n- IndexedDB 복원이 끝나기 전까지 사용자 입력을 막는 짧은 로딩 상태를 두는 것을 고려.\r\n\r\n---\r\n\r\n## 6. 🟠 [High] CSTS \"미리보기\" 페이지(`#cstsPage`) — 죽은 코드 (Dead Code), 회귀는 아니지만 정리 필요\r\n\r\n### 위치\r\n`www/index.html` `<section id=\"cstsPage\" ...>`, `www/script.js`의 `currentCstsSet`, `renderCstsSelect`, `renderCstsFigure`, `renderCstsOptions`, `renderCstsPage`, 그리고 `cstsSetSelect`/`cstsPrevBtn`/`cstsNextBtn` 이벤트 리스너.\r\n\r\n### 문제\r\n`openCstsApp()`은 다음과 같이 구현되어 있습니다:\r\n\r\n```js\r\nfunction openCstsApp() {\r\n  startProduct(\"csts\");\r\n  showActiveProductApp();   // appShell(메인 퀴즈 UI)만 보여줌\r\n  questionTitle?.focus?.();\r\n}\r\n\r\nfunction showActiveProductApp() {\r\n  productGate?.classList.add(\"is-product-hidden\");\r\n  productGate?.setAttribute(\"hidden\", \"\");\r\n  cstsPage?.classList.add(\"is-product-hidden\");   // cstsPage는 항상 숨김\r\n  cstsPage?.setAttribute(\"hidden\", \"\");\r\n  appShell?.classList.remove(\"is-product-hidden\");\r\n}\r\n```\r\n\r\n즉 CSTS 버튼을 눌러도 **`#cstsPage`(전용 미리보기 UI)는 절대 표시되지 않고**, ISTQB와 동일한 `appShell`이 CSTS 데이터로 렌더링됩니다. (이 동작은 `main`에서도 동일했으므로 이번 브랜치의 회귀는 아닙니다.)\r\n\r\n그 결과:\r\n- `renderCstsPage()`, `renderCstsSelect()`, `renderCstsFigure()`, `renderCstsOptions()`, `currentCstsSet()` — 약 100여 줄의 함수와 `cstsState`, 그리고 `index.html`의 `#cstsPage` 섹션 전체(태그, 버튼, select)가 **호출되지 않는 죽은 코드**입니다.\r\n- `cstsSetSelect`, `cstsPrevBtn`, `cstsNextBtn`에 등록된 이벤트 리스너도 절대 발생하지 않음.\r\n\r\n### 영향\r\n- 코드베이스 크기 증가, 신규 기여자의 혼란 가능성(\"이 페이지는 언제 쓰이지?\").\r\n- 향후 데이터 구조가 바뀌면 이 죽은 코드도 같이 수정해야 한다고 오인하여 불필요한 작업이 발생할 수 있음.\r\n\r\n### 권장 조치\r\n- `#cstsPage` 관련 HTML/CSS/JS를 완전히 제거하거나,\r\n- 혹은 \"간단 미리보기 모드\"로 의도적으로 부활시켜 실제로 연결할지 결정 필요 (제품 결정 사항).\r\n\r\n---\r\n\r\n## 7. 🟠 [High] 설정 패널(`.settings-content`)이 모바일 사이드바에서 잘림 + 외부 클릭 닫기 없음\r\n\r\n### 위치\r\n`www/style.css`, `www/script.js` (`settingsPanelToggleBtn` 리스너)\r\n\r\n### 코드\r\n\r\n```css\r\n/* branch에서 변경됨: static → absolute */\r\n.settings-content {\r\n  position: absolute;\r\n  top: calc(100% + 8px);\r\n  left: 0;\r\n  right: 0;\r\n  z-index: 80;\r\n  max-height: min(70vh, 560px);\r\n  overflow-y: auto;\r\n  ...\r\n}\r\n```\r\n\r\n```css\r\n/* 태블릿 세로 이하(≤900px): 펼쳐진 사이드바 */\r\n.sidebar:not(.mobile-collapsed) {\r\n  max-height: min(58vh, 560px);\r\n  overflow-y: auto;   /* ← absolute 자식이 여기서 클리핑됨 */\r\n}\r\n```\r\n\r\n```js\r\nsettingsPanelToggleBtn.addEventListener(\"click\", () => {\r\n  const isOpen = settingsPanelToggleBtn.getAttribute(\"aria-expanded\") === \"true\";\r\n  settingsPanelToggleBtn.setAttribute(\"aria-expanded\", String(!isOpen));\r\n  settingsPanel.hidden = isOpen;\r\n});\r\n// 패널 바깥 클릭 시 닫는 핸들러 없음\r\n```\r\n\r\n### 문제\r\n1. `.settings-content`는 `position: absolute`이고 부모 체인에 `overflow-y: auto`인 `.sidebar:not(.mobile-collapsed)`가 있어, 패널이 사이드바의 `max-height`(58vh)를 넘으면 **잘려서 일부 버튼(초기화, 기록 관리 등)에 접근할 수 없음**.\r\n2. 패널 외부를 클릭/탭해도 닫히지 않아, 모바일에서 열어둔 패널이 화면을 계속 가림.\r\n\r\n### 권장 조치\r\n- 모바일 브레이크포인트에서는 `.settings-content`를 모달(`.modal-backdrop` 패턴)로 전환하거나, `position: fixed` + 자체 스크롤 컨테이너로 변경.\r\n- `document.addEventListener(\"click\", ...)`로 외부 클릭 시 `settingsPanel.hidden = true` 처리 추가.\r\n\r\n---\r\n\r\n## 8. 🟠 [High] `.figure-zoom-btn`이 가로 스크롤 컨테이너에서 잘림\r\n\r\n### 위치\r\n`www/style.css`\r\n\r\n```css\r\n.question-figure {\r\n  position: relative;\r\n  margin: 4px 0 8px;\r\n  overflow-x: auto;     /* ← absolute 자식을 클리핑 */\r\n}\r\n\r\n.figure-zoom-btn {\r\n  position: absolute;\r\n  right: 10px;\r\n  bottom: 10px;\r\n  ...\r\n}\r\n```\r\n\r\n### 문제\r\n`.question-figure img`는 `max-width: min(100%, 760px)`이라 컨테이너보다 클 수 있고, 이 경우 `.question-figure`에 가로 스크롤이 생깁니다. `.figure-zoom-btn`은 `absolute`로 컨테이너 우하단에 고정되도록 의도되었지만, `overflow-x: auto`인 부모 안에서는 **스크롤 가능한 콘텐츠 영역 기준으로 위치가 계산**되어 화면에 보이지 않거나 스크롤해야만 보이는 위치에 배치될 수 있습니다.\r\n\r\n### 권장 조치\r\n- 확대 버튼을 `.question-figure` 바깥의 별도 wrapper(`position: relative`, `overflow: visible`)에 `position: sticky` 또는 `fixed` 형태로 배치.\r\n- 또는 이미지 자체에 `cursor: zoom-in`만 유지하고, 확대 버튼은 이미지 위에 오버레이가 아닌 이미지 아래 별도 영역에 고정 배치.\r\n\r\n---\r\n\r\n## 9. 🟠 [High] 문제 번호 네비게이션(`#questionNav`)에 `scrollIntoView` 없음\r\n\r\n### 위치\r\n`www/script.js` `renderNav(questions)`\r\n\r\n```js\r\nfunction renderNav(questions) {\r\n  questionNav.replaceChildren();\r\n  const fragment = document.createDocumentFragment();\r\n  questions.forEach((question, index) => {\r\n    const button = document.createElement(\"button\");\r\n    ...\r\n    button.classList.toggle(\"current\", index === state.index);\r\n    ...\r\n    fragment.appendChild(button);\r\n  });\r\n  questionNav.appendChild(fragment);\r\n  // 현재 문제 버튼을 보이는 영역으로 스크롤하는 로직 없음\r\n}\r\n```\r\n\r\n### 문제\r\n`.question-nav`는 고정된 그리드(`repeat(20, minmax(34px, 1fr))` 등)이며, 컨테이너 자체는 스크롤 가능한 영역(`.sidebar` 등) 안에 위치합니다. \"이전/다음\" 버튼이나 키보드 탐색으로 `state.index`가 바뀌어도, 현재 선택된 `.current` 버튼이 화면 밖에 있으면 **사용자가 스크롤해야만 현재 위치를 확인**할 수 있습니다.\r\n\r\n### 권장 조치\r\n```js\r\nconst currentBtn = questionNav.querySelector(\".current\");\r\ncurrentBtn?.scrollIntoView({ block: \"nearest\", inline: \"nearest\" });\r\n```\r\n`renderNav()` 끝부분에 추가.\r\n\r\n---\r\n\r\n## 10. 🟡 [Medium] 신규 컴포넌트들이 다크모드(`prefers-color-scheme: dark`)를 지원하지 않음\r\n\r\n### 위치\r\n`www/style.css`\r\n\r\n| 컴포넌트 | 하드코딩 색상 | 비고 |\r\n|---|---|---|\r\n| `.question-stem .note-line` | `#b8cbc5`(테두리), `#f8fbfa`(배경) | 다크 배경에서 흰 박스로 표시 |\r\n| `.multi-answer-badge` | `#f0c96a`, `linear-gradient(90deg, #fff9e6, #fffdf5)`, `#7a5200` | 다크모드에서 가독성 저하 |\r\n| `.data-table`, `.table-scroll`, `.table-hint` | `#c8d7d2`, `#fbfdfc`, `#eef7f4`, `#dce7e3` | 흰 테이블이 다크 배경 위에 튀어 보임 |\r\n\r\n### 문제\r\n`@media (prefers-color-scheme: dark) { ... }` 블록이 존재하지만, 위 3개 컴포넌트는 모두 **이번 브랜치에서 신규/변경된 것**이며 다크모드 오버라이드 목록에 포함되어 있지 않습니다.\r\n\r\n### 권장 조치\r\n다크모드 미디어 쿼리 안에 다음과 같은 오버라이드 추가:\r\n```css\r\n@media (prefers-color-scheme: dark) {\r\n  .question-stem .note-line {\r\n    border-left-color: var(--border);\r\n    background: var(--secondary);\r\n    color: var(--muted-foreground);\r\n  }\r\n  .multi-answer-badge {\r\n    border-color: rgba(240, 187, 104, 0.4);\r\n    background: rgba(240, 187, 104, 0.12);\r\n    color: var(--warning);\r\n  }\r\n  .table-scroll, .data-table, .data-table th {\r\n    border-color: var(--border);\r\n    background: var(--card);\r\n  }\r\n}\r\n```\r\n\r\n---\r\n\r\n## 11. 🟡 [Medium] `note-line` 스타일이 `.question-stem`에만 적용, `.option-text`/`.feedback`에는 누락\r\n\r\n### 위치\r\n`www/style.css`\r\n\r\n```css\r\n.question-stem .note-line { ... }\r\n/* .option-text .note-line, .feedback .note-line 없음 */\r\n```\r\n\r\n### 문제\r\n`normalizeQuestionBlock()`(branch 신규 함수)은 `type: \"note\"` 블록을 어디서든(문제 본문, 선택지, 해설) 생성할 수 있도록 일반화되어 있으나, CSS는 `.question-stem` 컨텍스트만 스타일링합니다. 선택지나 해설에 `note` 블록이 포함된 문제 데이터가 들어오면 일반 텍스트와 시각적으로 구분되지 않습니다.\r\n\r\n### 권장 조치\r\n셀렉터를 `.question-stem .note-line, .option-text .note-line, .feedback .note-line`으로 확장.\r\n\r\n---\r\n\r\n## 12. 🟡 [Medium] 고아(orphan) CSS — 제거된 \"긴 지문 접기/펼치기\" 기능의 잔존 스타일\r\n\r\n### 위치\r\n`www/style.css` (다음 클래스 약 40줄, 모바일 미디어쿼리 내 중복分 포함)\r\n```css\r\n.question-stem.stem-collapsed { ... }\r\n.stem-toggle-btn { ... }\r\n.stem-toggle-btn:hover { ... }\r\n.stem-toggle-btn .stem-toggle-icon { ... }\r\n.stem-toggle-btn.expanded .stem-toggle-icon { ... }\r\n```\r\n\r\n`www/script.js`:\r\n```js\r\n// 긴 지문 접기/펼치기 기능 제거됨\r\ndocument.querySelector(\".stem-toggle-btn\")?.remove();\r\nquestionStem.classList.remove(\"stem-collapsed\");\r\n```\r\n\r\n### 문제\r\nJS에서는 기능이 의도적으로 제거되었음을 주석으로 명시했지만, 해당 클래스를 생성하는 코드가 없으므로 CSS 규칙은 영구적으로 매치되지 않는 죽은 코드입니다.\r\n\r\n### 권장 조치\r\nCSS에서 위 셀렉터 전부 삭제 (모바일 미디어쿼리 내 중복 정의 포함).\r\n\r\n---\r\n\r\n## 13. 🟡 [Medium] 오답 노트 필터 버튼 — 인라인 스타일로 구현되어 다크모드/반응형 미적용\r\n\r\n### 위치\r\n`www/script.js` `renderWrongNote()` 내부 `createFilterBtn`\r\n\r\n```js\r\nconst btn = document.createElement(\"button\");\r\nbtn.type = \"button\";\r\nbtn.textContent = text;\r\nbtn.style.whiteSpace = \"nowrap\";\r\nbtn.style.padding = \"8px 16px\";\r\nbtn.style.borderRadius = \"20px\";\r\nbtn.style.minHeight = \"36px\";\r\nif (wrongNoteFilter === value) {\r\n  btn.className = \"primary\";\r\n} else {\r\n  btn.className = \"subtle\";\r\n  btn.style.border = \"1px solid var(--line)\";\r\n}\r\n```\r\n\r\n### 문제\r\n- `btn.style.border = \"1px solid var(--line)\"`처럼 인라인 스타일에 CSS 변수를 사용하는 것은 동작은 하지만, 반응형 브레이크포인트별 오버라이드(`@media`)가 불가능합니다.\r\n- 클래스 기반 스타일링과 혼용되어 유지보수 시 스타일 출처 파악이 어려움.\r\n\r\n### 권장 조치\r\n`.wrong-note-filter-btn` 클래스를 `style.css`에 정의하고, JS에서는 `btn.classList.add(\"wrong-note-filter-btn\", isActive ? \"active\" : \"\")` 형태로 전환.\r\n\r\n---\r\n\r\n## 14. 🟡 [Medium] `.wrong-note-filters` 가로 스크롤에 시각적 힌트 없음\r\n\r\n### 위치\r\n`www/style.css`\r\n\r\n```css\r\n.wrong-note-filters {\r\n  display: flex;\r\n  gap: 8px;\r\n  overflow-x: auto;\r\n  padding-bottom: 10px;\r\n  margin-bottom: 10px;\r\n  scrollbar-width: none;     /* 스크롤바 숨김 */\r\n}\r\n.wrong-note-filters::-webkit-scrollbar {\r\n  display: none;\r\n}\r\n```\r\n\r\n### 문제\r\n채점 기록(`state.histories`)이 많아질수록(최대 30개) 필터 버튼이 가로로 길게 늘어나지만, 스크롤바를 완전히 숨겨서 \"더 많은 기록이 있다\"는 시각적 신호가 전혀 없습니다. 사용자가 최근 몇 개의 기록만 보고 더 이전 기록이 있다는 것을 인지하지 못할 수 있습니다.\r\n\r\n### 권장 조치\r\n좌우 끝에 `mask-image`/`box-shadow` 페이드 효과를 추가하거나, 좌우 화살표 버튼을 제공.\r\n\r\n---\r\n\r\n## 15. 🟡 [Medium] `resetAnswersFor()` — 답안 초기화 시 히스토리(`state.histories`)는 유지됨 (의도 확인 필요)\r\n\r\n### 위치\r\n`www/script.js`\r\n\r\n```js\r\nfunction resetAnswersFor(mode) {\r\n  const questions = mode === \"random\" ? randomQuestions() : currentSet().questions;\r\n  questions.forEach((question) => {\r\n    delete state.answers[answerKey(question, mode)];\r\n    delete state.answers[legacyAnswerKey(question, mode)];\r\n  });\r\n}\r\n```\r\n\r\n### 확인 필요 사항\r\n\"현재 모드 풀이 초기화\"(`resetBtn`) 시 `state.answers`만 삭제하고 `state.histories`(이전 채점 기록)는 그대로 남습니다. 이는 \"오답 노트\"가 과거 채점 시점의 기록을 보존한다는 점에서 의도된 동작일 수 있으나, 위 **#1 버그**와 결합하면 \"초기화 후에도 오답 노트에 (잘못된) 전체 오답 기록이 계속 누적되어 표시\"되는 결과로 이어집니다. #1을 수정할 때 이 흐름도 함께 재검토 필요.\r\n\r\n---\r\n\r\n## 16. ⚪ [Low] `verify.js`의 `questions.js` 참조 금지 검사 — 메시지 보강 권장\r\n\r\n### 위치\r\n`scripts/verify.js`\r\n\r\n```js\r\nconst html = fs.readFileSync(path.join(root, \"www\", \"index.html\"), \"utf8\");\r\nif (/questions\\.js|csts-questions\\.js/.test(html)) {\r\n  throw new Error(\"www/index.html must not load questions.js or csts-questions.js\");\r\n}\r\n```\r\n\r\n### 의견\r\n이번 브랜치의 데이터 로딩 방식 전환(`<script src=\"./questions.js\">` → `fetch(\"./data/index.json\")`)을 강제하기 위한 좋은 가드입니다. 다만 에러 메시지에 \"데이터는 `data/index.json`을 통해 fetch로 로드해야 합니다\" 같은 **대안 안내**를 추가하면 향후 기여자가 원인을 더 빨리 파악할 수 있습니다.\r\n\r\n---\r\n\r\n## 17. ⚪ [Low] `figureFor()`의 그림 매핑 테이블이 하드코딩되어 데이터 주도(Data-driven)가 아님\r\n\r\n### 위치\r\n`www/script.js` `figureFor(question)`\r\n\r\n```js\r\nfunction figureFor(question) {\r\n  if (question.figure) return question.figure;\r\n  const setId = question.setId || state.setId;\r\n  const sampleCode = String(setId || \"\").split(\"-\").pop();\r\n  const key = `${sampleCode}${question.number}`;\r\n  const figures = {\r\n    A23: \"figures/A23.png\",\r\n    B23: \"figures/B23.png\",\r\n    C23: \"figures/C23.png\",\r\n    C24: \"figures/C24.png\",\r\n    C31: \"figures/C31.png\",\r\n    C32: \"figures/C32.png\",\r\n  };\r\n  return figures[key] || \"\";\r\n}\r\n```\r\n\r\n### 의견\r\n`question.figure` 필드가 이미 존재하여 우선 사용되므로, 이 하드코딩 테이블은 `figure` 필드가 비어 있는 레거시 문제들을 위한 fallback으로 보입니다. `audit-phase3-content.js`에서 그림이 필요한 문제 ID 목록(`imageRequired`)을 관리하고 있으므로, 장기적으로는 모든 문제 JSON에 `figure` 필드를 채워 이 하드코딩 테이블을 제거하는 것이 데이터 일관성 측면에서 바람직합니다.\r\n\r\n---\r\n\r\n## 18. ⚪ [Low] `historyAnswers` 키 필터링 방식 자체의 설계 재검토 권장 (#1과 연계)\r\n\r\n`#1`의 직접적 수정과 별개로, \"현재 세트/모드의 답안만 골라서 히스토리에 저장\"하는 방식 자체가 `state.answers`의 **전체 키를 문자열 패턴 매칭으로 필터링**하는 구조입니다. 이는 `answerKey()`의 포맷이 바뀔 때마다(이번처럼) 조용히 깨질 수 있는 취약한 설계입니다.\r\n\r\n장기적으로는 \"현재 세트/모드에 속한 질문 목록(`currentSet().questions` 또는 `randomQuestions()`)을 순회하며 `answerKey(question, mode)`로 직접 키를 계산\"하는 방식으로 전환하면, 키 포맷이 바뀌어도 히스토리 저장 로직이 깨지지 않습니다 (#1의 제안과 동일).\r\n\r\n---\r\n\r\n## 부록: 파일별 변경 통계 (main 대비)\r\n\r\n| 파일 | 변경 | 비고 |\r\n|---|---|---|\r\n| `www/script.js` | 3333 → 3544줄 (+211) | 데이터 로딩 방식 전환, 오답 노트, 타이머, 그림 키 수정 등 |\r\n| `www/style.css` | 변경 ~30곳 | 설정 패널, note-line, multi-answer-badge, 테이블 등 신규 스타일 |\r\n| `www/index.html` | `<script src=\"./questions.js\">`, `<script src=\"./csts-questions.js\">` 제거 | 신규 fetch 기반 로딩과 일치 |\r\n\r\n---\r\n\r\n## 우선순위 권장 작업 순서\r\n\r\n1. **#1 오답 노트 키 불일치** — 핵심 기능 정상화 (최우선)\r\n2. **#7, #8, #9** — 모바일 UI 클리핑/스크롤 문제 (사용성 직접 영향)\r\n3. **#5** — IndexedDB 복원 경쟁 조건 (재현 어렵지만 영향 큼)\r\n4. **#4** — 랜덤 모드 40문항 가드 (현재는 안전하지만 잠재적 회귀 방지)\r\n5. **#10, #11** — 다크모드/스타일 일관성\r\n6. **#6, #12, #13, #14, #15, #16~18** — 정리성 작업, 여유 있을 때 진행\r\n\n</USER_REQUEST>\n<ADDITIONAL_METADATA>\nThe current local time is: 2026-06-14T13:54:10+09:00.\n\nThe user has mentioned some items in the form @[ITEM]. Here is extra information about the items that were mentioned by the user, in the order that they appear:\n\n/goal is a [Slash Command]:\nThe user has marked this task with /goal, indicating that this task is intended to run for a long time without user input, e.g. overnight. You should be extra thorough and only stop when you are confident the goal has been completely fulfilled. The system will force you to continue execution, prompting you to audit your work until completion.\n</ADDITIONAL_METADATA>"

"<USER_REQUEST>\n/goal 시작 전 너가 보기에 중요한 UI, 코드, 데이터 정합성, 최적화 방면에서 각각 10가지씩 만족하는 규칙을 세워서 모두 완료하고\r\n\r\n테스트 검즘까지 ALL PASS 가 나야해\n\n아래의 리뷰를 보고 진행해\n\n# `codex/fix-issues-in-phase-4` 브랜치 코드 리뷰 리포트\r\n\r\n대상 저장소: `WindowHyun/ISTQB`\r\n대상 브랜치: `codex/fix-issues-in-phase-4`\r\n비교 기준: `main`\r\n대상 파일: `www/script.js`, `www/style.css`, `www/index.html`\r\n\r\n---\r\n\r\n## 0. 요약\r\n\r\n| 등급 | 개수 | 설명 |\r\n|---|---|---|\r\n| 🔴 Critical | 5건 | 핵심 기능이 동작하지 않거나 데이터가 손상될 수 있는 버그 |\r\n| 🟠 High | 4건 | 특정 조건에서 화면/동작이 명백히 잘못되는 문제 |\r\n| 🟡 Medium | 6건 | UX 저하, 접근성/다크모드 누락, 잠재적 회귀 |\r\n| ⚪ Low | 3건 | 코드 정리, 유지보수성 |\r\n\r\n가장 심각한 항목은 **#1 오답 노트 기능 자체가 동작하지 않는 버그**입니다. 이번 브랜치의 핵심 신규 기능(README에 명시된 \"오답 다시풀기\" / \"오답 노트\")이 채점 결과를 전혀 반영하지 못합니다.\r\n\r\n---\r\n\r\n## 1. 🔴 [Critical] 오답 노트 기능이 항상 \"전부 오답\"으로 표시됨\r\n\r\n### 위치\r\n`www/script.js` — 채점(`gradeBtn` 클릭) 핸들러 내부, `historyAnswers` 생성 부분.\r\n\r\n### 코드\r\n\r\n```js\r\n// 채점 시 히스토리에 저장할 답안을 추출하는 부분\r\nconst historyAnswers = {};\r\nObject.keys(state.answers).forEach(key => {\r\n  if (key.includes(`-${targetMode}-`)) {\r\n     historyAnswers[key] = state.answers[key];\r\n  }\r\n});\r\n\r\nstate.histories.push({\r\n  id: timestamp.toString(),\r\n  timestamp,\r\n  mode: state.mode,\r\n  setId: targetSetId,\r\n  answers: historyAnswers,\r\n  randomRefs: state.mode === \"random\" ? [...state.randomRefs] : null\r\n});\r\n```\r\n\r\n### 문제\r\n\r\n이번 브랜치에서 `answerKey()`가 다음과 같이 변경되었습니다 (Phase 4의 핵심 개선 중 하나):\r\n\r\n```js\r\nfunction answerKey(question, mode = answerMode()) {\r\n  if (question.id) return `${question.id}-${mode}`;   // 신규 포맷: \"ISTQB-FL-V4-A-001-exam\"\r\n  return legacyAnswerKey(question, mode);\r\n}\r\n\r\nfunction legacyAnswerKey(question, mode = answerMode()) {\r\n  const setId = question.legacySetId || question.setId || state.setId;\r\n  return `${setId}-${mode}-${question.number}`;       // 구 포맷: \"ISTQB-FL-V4-A-exam-1\"\r\n}\r\n```\r\n\r\n실제 데이터(`www/data/istqb/sample-a.json` 등)의 모든 문제에는 `id` 필드가 존재합니다 (`\"id\": \"ISTQB-FL-V4-A-001\"`). 따라서 `chooseOption()` → `saveAnswers()`로 저장되는 실제 키는 항상 **신규 포맷** `\"ISTQB-FL-V4-A-001-exam\"` 형태입니다.\r\n\r\n그런데 히스토리 생성 시 필터는 `key.includes(`-${targetMode}-`)`, 즉 **`\"-exam-\"`(앞뒤로 하이픈)이 포함되어야** 함을 요구합니다.\r\n\r\n```text\r\n신규 포맷: \"ISTQB-FL-V4-A-001-exam\"  → \"-exam-\" 포함?  ❌ (끝이 \"-exam\"으로 끝나서 뒤에 \"-\"가 없음)\r\n구 포맷:   \"ISTQB-FL-V4-A-exam-1\"    → \"-exam-\" 포함?  ✅\r\n```\r\n\r\n결과적으로 **`historyAnswers`는 항상 빈 객체 `{}`** 가 됩니다.\r\n\r\n이후 오답 노트를 그릴 때:\r\n\r\n```js\r\nfunction historyWrongNoteItems(history) {\r\n  ...\r\n  questions.forEach((question) => {\r\n     const ansKey = `${question.setId || history.setId}-${history.mode}-${question.number}`; // 구 포맷 키로 조회\r\n     const selected = history.answers[ansKey] || [];   // 항상 [] (history.answers === {})\r\n     const isCor =\r\n       question.type === \"short_answer\"\r\n         ? normalizeTextAnswer(selected[0]) === normalizeTextAnswer(question.answer[0])\r\n         : sameChoices(selected, question.answer);      // sameChoices([], [...]) → 항상 false\r\n\r\n     if (!isCor) {\r\n       items.push({ ... });   // 모든 문제가 여기로 들어감\r\n     }\r\n  });\r\n}\r\n```\r\n\r\n`selected`가 항상 빈 배열이므로 `isCor`는 모든 문제에 대해 `false`가 되고, **채점 결과와 무관하게 세트 내 모든 문제(예: 40문항)가 오답 노트에 표시**됩니다.\r\n\r\n### 영향\r\n- README에서 강조한 핵심 기능(\"오답 다시풀기를 누르기 전까지 기존 오답 기록 보호\", \"오답 노트\")이 사실상 무의미해짐.\r\n- `오답 다시풀기`(`retryWrongBtn`)는 `missedExamQuestions()`를 사용하므로 별도 경로이고 정상 동작하지만, **오답 노트 모달에 표시되는 목록과 실제 채점 결과가 일치하지 않음** → 사용자에게 혼란을 줌.\r\n\r\n### 수정 방향 (제안)\r\n`historyAnswers` 필터와 `historyWrongNoteItems`의 조회 키를 모두 `answerKey(question, mode)` 기준(신규 포맷)으로 통일해야 합니다.\r\n\r\n```js\r\n// 필터: question 객체 기준으로 직접 키를 계산\r\nconst targetQuestions =\r\n  state.mode === \"random\" ? randomQuestions() : currentSet().questions;\r\n\r\nconst historyAnswers = {};\r\ntargetQuestions.forEach((question) => {\r\n  const key = answerKey(question, targetMode);\r\n  if (state.answers[key]) historyAnswers[key] = state.answers[key];\r\n});\r\n```\r\n\r\n```js\r\n// historyWrongNoteItems 조회도 동일하게\r\nconst ansKey = answerKey(question, history.mode === \"random\" ? \"random\" : \"exam\");\r\nconst selected = history.answers[ansKey] || [];\r\n```\r\n\r\n---\r\n\r\n## 2. 🔴 [Critical] `figureFor()` 키 생성 버그 — 그림이 표시되지 않음\r\n\r\n### 위치\r\n`www/script.js` `figureFor(question)`\r\n\r\n### 코드 (main vs branch)\r\n\r\n```js\r\n// main (버그)\r\nconst setId = question.setId || state.setId;\r\nconst key = `${setId}${question.number}`;   // \"ISTQB-FL-V4-A23\" → 매핑 테이블 키와 불일치\r\n\r\n// branch (수정 완료)\r\nconst sampleCode = String(setId || \"\").split(\"-\").pop();\r\nconst key = `${sampleCode}${question.number}`;  // \"A23\" → 매핑 테이블과 일치\r\n```\r\n\r\n### 상태\r\n✅ **이 브랜치에서 이미 수정됨.** (참고용으로 기록 — 회귀 방지를 위해 테스트 케이스 추가를 권장)\r\n\r\n---\r\n\r\n## 3. 🔴 [Critical] 타이머 백그라운드 복귀 시 폭증 — `elapsedSeconds` 방식으로 수정됨\r\n\r\n### 위치\r\n`www/script.js` `updateTimer()`, `state.elapsedSeconds` / `state.lastTick`\r\n\r\n### 상태\r\n✅ **이 브랜치에서 절대시각(`startedAt`) 방식 → 누적시간(`elapsedSeconds`) + `lastTick` 델타 방식으로 수정됨.**\r\n\r\n다만 아래 **#7 IndexedDB 복원 경쟁 조건**과 함께 검토 필요 (관련 상태가 비동기 복원 대상에 포함됨).\r\n\r\n---\r\n\r\n## 4. 🔴 [Critical] `randomQuestions()` — 전체 문항 수가 40개 미만이면 매 렌더마다 무한 재셔플\r\n\r\n### 위치\r\n`www/script.js`\r\n\r\n```js\r\nfunction generateRandomRefs() {\r\n  state.randomRefs = shuffle(allQuestions())\r\n    .slice(0, 40)\r\n    .map((question) => ({ setId: question.setId, number: question.number }));\r\n}\r\n\r\nfunction randomQuestions() {\r\n  if (state.randomRefs.length !== 40) generateRandomRefs();  // ⚠️\r\n  return state.randomRefs\r\n    .map((ref) => { ... })\r\n    .filter(Boolean);\r\n}\r\n```\r\n\r\n### 문제\r\n`allQuestions()`가 반환하는 전체 문항 수가 40개 미만인 제품/세트 구성이라면:\r\n\r\n- `shuffle(allQuestions()).slice(0, 40)`은 **40개보다 적은 배열**을 반환합니다.\r\n- `state.randomRefs.length !== 40` 조건이 **항상 true**가 되어,\r\n- `randomQuestions()`가 호출될 때마다(즉 `render()`가 호출될 때마다) `generateRandomRefs()`가 다시 실행됩니다.\r\n\r\n결과적으로 **랜덤 모드에 진입한 직후부터 매 렌더링마다 문제 순서가 무작위로 재섹션**됩니다. 사용자가 답을 선택해 `render()`가 다시 호출되는 순간 `currentQuestions()`(=`randomQuestions()`)의 순서/구성이 바뀌면서:\r\n- 현재 보고 있던 문제가 다른 문제로 바뀌거나,\r\n- `state.index`가 가리키는 문제가 매번 달라짐.\r\n\r\n### 현재 안전한 이유\r\n현재 ISTQB(186문항)·CSTS(각 세트 수십 문항 합산) 데이터셋은 합계가 40을 넘기 때문에 **지금 당장은 발생하지 않음**. 그러나:\r\n- 향후 세트를 줄이거나, 특정 제품에 문항이 적은 카테고리를 추가하면 즉시 재현되는 **잠재적 타임 폭탄(time bomb)**.\r\n- 전체 문항 수가 정확히 40개 이상인지 검증하는 가드가 코드 어디에도 없음.\r\n\r\n### 수정 방향 (제안)\r\n```js\r\nfunction randomQuestions() {\r\n  const targetCount = Math.min(40, allQuestions().length);\r\n  if (state.randomRefs.length !== targetCount || state.randomRefs.length === 0) {\r\n    generateRandomRefs();\r\n  }\r\n  ...\r\n}\r\n```\r\n혹은 최소한 `allQuestions().length < 40`일 때 경고 로그 + 가능한 전체 문항 수로 대체.\r\n\r\n---\r\n\r\n## 5. 🔴 [Critical] `restorePersistentSnapshot()`의 비동기 경쟁 조건 (Race Condition)\r\n\r\n### 위치\r\n`www/script.js`\r\n\r\n```js\r\n// 초기화 마지막 부분\r\nrenderExamSelect();\r\nrenderMode();\r\nrender();\r\nrestorePersistentSnapshot();   // await 하지 않음 (fire-and-forget)\r\n```\r\n\r\n```js\r\nasync function restorePersistentSnapshot() {\r\n  let snapshot = null;\r\n  let hasUiState = false;\r\n  try {\r\n    snapshot = JSON.parse(localStorage.getItem(persistenceKey()));\r\n    hasUiState = Boolean(localStorage.getItem(uiStorageKey()));   // ← 이 시점에 동기적으로 평가\r\n  } catch { snapshot = null; }\r\n\r\n  const db = await openPersistenceDb();          // ← IndexedDB open은 비동기, 수~수십ms 소요 가능\r\n  if (db) {\r\n    try {\r\n      snapshot = await new Promise((resolve) => { ... });\r\n    } catch { ... }\r\n  }\r\n  if (!snapshot || typeof snapshot !== \"object\") return;\r\n  if (snapshot.answers && Object.keys(state.answers).length === 0) {\r\n    state.answers = sanitizeAnswerState(snapshot.answers);\r\n  }\r\n  if (snapshot.uiState && !hasUiState) {\r\n    applyBackupSnapshot({ answers: state.answers, uiState: snapshot.uiState });  // ← 전체 state 덮어씀\r\n  }\r\n  renderExamSelect();\r\n  renderMode();\r\n  render();\r\n}\r\n```\r\n\r\n### 문제\r\n1. `hasUiState`는 **함수 진입 시점**(IndexedDB 열기 이전)의 `localStorage` 상태로 결정됩니다.\r\n2. 만약 사용자가 **앱 최초 진입 직후 즉시** (IndexedDB가 열리기 전, 수~수십ms 사이) 옵션을 클릭하면:\r\n   - `chooseOption()` → `saveAnswers()` + `saveUiState()` 실행 → `localStorage`에 `uiState`/`answers`가 기록됨.\r\n   - 그러나 `hasUiState` 변수는 이미 `false`로 캡처되어 있으므로, 이후 IndexedDB에서 가져온 **오래된 snapshot.uiState로 사용자의 첫 클릭 상태가 덮어써짐(`applyBackupSnapshot`)**.\r\n3. `state.answers` 조건은 `Object.keys(state.answers).length === 0`으로 체크하므로 답안 자체는 비교적 안전하지만, **`state.mode`, `state.index`, `examGraded`, `reviewRetake` 등 UI 상태 전체가 스냅샷으로 되돌아갈 수 있음** — 예: 사용자가 \"시험 모드\"로 전환했는데 다시 \"연습 모드\"로 되돌아가는 식의 깜빡임/되돌림 현상.\r\n\r\n### 영향\r\n- 실제 발생 빈도는 낮지만(타이밍 윈도우가 매우 짧음), 느린 기기나 IndexedDB 초기화가 지연되는 환경(특히 Android WebView)에서는 재현 가능성이 더 높음.\r\n- 디버깅이 매우 어려운 \"가끔 처음 클릭이 무시되는 것처럼 보이는\" 버그로 보고될 가능성.\r\n\r\n### 수정 방향 (제안)\r\n- 앱 부팅 시 `restorePersistentSnapshot()`을 **`render()` 이전에 `await`** 하거나,\r\n- `hasUiState`를 `restorePersistentSnapshot` 완료 시점에 다시 평가(재확인)하거나,\r\n- IndexedDB 복원이 끝나기 전까지 사용자 입력을 막는 짧은 로딩 상태를 두는 것을 고려.\r\n\r\n---\r\n\r\n## 6. 🟠 [High] CSTS \"미리보기\" 페이지(`#cstsPage`) — 죽은 코드 (Dead Code), 회귀는 아니지만 정리 필요\r\n\r\n### 위치\r\n`www/index.html` `<section id=\"cstsPage\" ...>`, `www/script.js`의 `currentCstsSet`, `renderCstsSelect`, `renderCstsFigure`, `renderCstsOptions`, `renderCstsPage`, 그리고 `cstsSetSelect`/`cstsPrevBtn`/`cstsNextBtn` 이벤트 리스너.\r\n\r\n### 문제\r\n`openCstsApp()`은 다음과 같이 구현되어 있습니다:\r\n\r\n```js\r\nfunction openCstsApp() {\r\n  startProduct(\"csts\");\r\n  showActiveProductApp();   // appShell(메인 퀴즈 UI)만 보여줌\r\n  questionTitle?.focus?.();\r\n}\r\n\r\nfunction showActiveProductApp() {\r\n  productGate?.classList.add(\"is-product-hidden\");\r\n  productGate?.setAttribute(\"hidden\", \"\");\r\n  cstsPage?.classList.add(\"is-product-hidden\");   // cstsPage는 항상 숨김\r\n  cstsPage?.setAttribute(\"hidden\", \"\");\r\n  appShell?.classList.remove(\"is-product-hidden\");\r\n}\r\n```\r\n\r\n즉 CSTS 버튼을 눌러도 **`#cstsPage`(전용 미리보기 UI)는 절대 표시되지 않고**, ISTQB와 동일한 `appShell`이 CSTS 데이터로 렌더링됩니다. (이 동작은 `main`에서도 동일했으므로 이번 브랜치의 회귀는 아닙니다.)\r\n\r\n그 결과:\r\n- `renderCstsPage()`, `renderCstsSelect()`, `renderCstsFigure()`, `renderCstsOptions()`, `currentCstsSet()` — 약 100여 줄의 함수와 `cstsState`, 그리고 `index.html`의 `#cstsPage` 섹션 전체(태그, 버튼, select)가 **호출되지 않는 죽은 코드**입니다.\r\n- `cstsSetSelect`, `cstsPrevBtn`, `cstsNextBtn`에 등록된 이벤트 리스너도 절대 발생하지 않음.\r\n\r\n### 영향\r\n- 코드베이스 크기 증가, 신규 기여자의 혼란 가능성(\"이 페이지는 언제 쓰이지?\").\r\n- 향후 데이터 구조가 바뀌면 이 죽은 코드도 같이 수정해야 한다고 오인하여 불필요한 작업이 발생할 수 있음.\r\n\r\n### 권장 조치\r\n- `#cstsPage` 관련 HTML/CSS/JS를 완전히 제거하거나,\r\n- 혹은 \"간단 미리보기 모드\"로 의도적으로 부활시켜 실제로 연결할지 결정 필요 (제품 결정 사항).\r\n\r\n---\r\n\r\n## 7. 🟠 [High] 설정 패널(`.settings-content`)이 모바일 사이드바에서 잘림 + 외부 클릭 닫기 없음\r\n\r\n### 위치\r\n`www/style.css`, `www/script.js` (`settingsPanelToggleBtn` 리스너)\r\n\r\n### 코드\r\n\r\n```css\r\n/* branch에서 변경됨: static → absolute */\r\n.settings-content {\r\n  position: absolute;\r\n  top: calc(100% + 8px);\r\n  left: 0;\r\n  right: 0;\r\n  z-index: 80;\r\n  max-height: min(70vh, 560px);\r\n  overflow-y: auto;\r\n  ...\r\n}\r\n```\r\n\r\n```css\r\n/* 태블릿 세로 이하(≤900px): 펼쳐진 사이드바 */\r\n.sidebar:not(.mobile-collapsed) {\r\n  max-height: min(58vh, 560px);\r\n  overflow-y: auto;   /* ← absolute 자식이 여기서 클리핑됨 */\r\n}\r\n```\r\n\r\n```js\r\nsettingsPanelToggleBtn.addEventListener(\"click\", () => {\r\n  const isOpen = settingsPanelToggleBtn.getAttribute(\"aria-expanded\") === \"true\";\r\n  settingsPanelToggleBtn.setAttribute(\"aria-expanded\", String(!isOpen));\r\n  settingsPanel.hidden = isOpen;\r\n});\r\n// 패널 바깥 클릭 시 닫는 핸들러 없음\r\n```\r\n\r\n### 문제\r\n1. `.settings-content`는 `position: absolute`이고 부모 체인에 `overflow-y: auto`인 `.sidebar:not(.mobile-collapsed)`가 있어, 패널이 사이드바의 `max-height`(58vh)를 넘으면 **잘려서 일부 버튼(초기화, 기록 관리 등)에 접근할 수 없음**.\r\n2. 패널 외부를 클릭/탭해도 닫히지 않아, 모바일에서 열어둔 패널이 화면을 계속 가림.\r\n\r\n### 권장 조치\r\n- 모바일 브레이크포인트에서는 `.settings-content`를 모달(`.modal-backdrop` 패턴)로 전환하거나, `position: fixed` + 자체 스크롤 컨테이너로 변경.\r\n- `document.addEventListener(\"click\", ...)`로 외부 클릭 시 `settingsPanel.hidden = true` 처리 추가.\r\n\r\n---\r\n\r\n## 8. 🟠 [High] `.figure-zoom-btn`이 가로 스크롤 컨테이너에서 잘림\r\n\r\n### 위치\r\n`www/style.css`\r\n\r\n```css\r\n.question-figure {\r\n  position: relative;\r\n  margin: 4px 0 8px;\r\n  overflow-x: auto;     /* ← absolute 자식을 클리핑 */\r\n}\r\n\r\n.figure-zoom-btn {\r\n  position: absolute;\r\n  right: 10px;\r\n  bottom: 10px;\r\n  ...\r\n}\r\n```\r\n\r\n### 문제\r\n`.question-figure img`는 `max-width: min(100%, 760px)`이라 컨테이너보다 클 수 있고, 이 경우 `.question-figure`에 가로 스크롤이 생깁니다. `.figure-zoom-btn`은 `absolute`로 컨테이너 우하단에 고정되도록 의도되었지만, `overflow-x: auto`인 부모 안에서는 **스크롤 가능한 콘텐츠 영역 기준으로 위치가 계산**되어 화면에 보이지 않거나 스크롤해야만 보이는 위치에 배치될 수 있습니다.\r\n\r\n### 권장 조치\r\n- 확대 버튼을 `.question-figure` 바깥의 별도 wrapper(`position: relative`, `overflow: visible`)에 `position: sticky` 또는 `fixed` 형태로 배치.\r\n- 또는 이미지 자체에 `cursor: zoom-in`만 유지하고, 확대 버튼은 이미지 위에 오버레이가 아닌 이미지 아래 별도 영역에 고정 배치.\r\n\r\n---\r\n\r\n## 9. 🟠 [High] 문제 번호 네비게이션(`#questionNav`)에 `scrollIntoView` 없음\r\n\r\n### 위치\r\n`www/script.js` `renderNav(questions)`\r\n\r\n```js\r\nfunction renderNav(questions) {\r\n  questionNav.replaceChildren();\r\n  const fragment = document.createDocumentFragment();\r\n  questions.forEach((question, index) => {\r\n    const button = document.createElement(\"button\");\r\n    ...\r\n    button.classList.toggle(\"current\", index === state.index);\r\n    ...\r\n    fragment.appendChild(button);\r\n  });\r\n  questionNav.appendChild(fragment);\r\n  // 현재 문제 버튼을 보이는 영역으로 스크롤하는 로직 없음\r\n}\r\n```\r\n\r\n### 문제\r\n`.question-nav`는 고정된 그리드(`repeat(20, minmax(34px, 1fr))` 등)이며, 컨테이너 자체는 스크롤 가능한 영역(`.sidebar` 등) 안에 위치합니다. \"이전/다음\" 버튼이나 키보드 탐색으로 `state.index`가 바뀌어도, 현재 선택된 `.current` 버튼이 화면 밖에 있으면 **사용자가 스크롤해야만 현재 위치를 확인**할 수 있습니다.\r\n\r\n### 권장 조치\r\n```js\r\nconst currentBtn = questionNav.querySelector(\".current\");\r\ncurrentBtn?.scrollIntoView({ block: \"nearest\", inline: \"nearest\" });\r\n```\r\n`renderNav()` 끝부분에 추가.\r\n\r\n---\r\n\r\n## 10. 🟡 [Medium] 신규 컴포넌트들이 다크모드(`prefers-color-scheme: dark`)를 지원하지 않음\r\n\r\n### 위치\r\n`www/style.css`\r\n\r\n| 컴포넌트 | 하드코딩 색상 | 비고 |\r\n|---|---|---|\r\n| `.question-stem .note-line` | `#b8cbc5`(테두리), `#f8fbfa`(배경) | 다크 배경에서 흰 박스로 표시 |\r\n| `.multi-answer-badge` | `#f0c96a`, `linear-gradient(90deg, #fff9e6, #fffdf5)`, `#7a5200` | 다크모드에서 가독성 저하 |\r\n| `.data-table`, `.table-scroll`, `.table-hint` | `#c8d7d2`, `#fbfdfc`, `#eef7f4`, `#dce7e3` | 흰 테이블이 다크 배경 위에 튀어 보임 |\r\n\r\n### 문제\r\n`@media (prefers-color-scheme: dark) { ... }` 블록이 존재하지만, 위 3개 컴포넌트는 모두 **이번 브랜치에서 신규/변경된 것**이며 다크모드 오버라이드 목록에 포함되어 있지 않습니다.\r\n\r\n### 권장 조치\r\n다크모드 미디어 쿼리 안에 다음과 같은 오버라이드 추가:\r\n```css\r\n@media (prefers-color-scheme: dark) {\r\n  .question-stem .note-line {\r\n    border-left-color: var(--border);\r\n    background: var(--secondary);\r\n    color: var(--muted-foreground);\r\n  }\r\n  .multi-answer-badge {\r\n    border-color: rgba(240, 187, 104, 0.4);\r\n    background: rgba(240, 187, 104, 0.12);\r\n    color: var(--warning);\r\n  }\r\n  .table-scroll, .data-table, .data-table th {\r\n    border-color: var(--border);\r\n    background: var(--card);\r\n  }\r\n}\r\n```\r\n\r\n---\r\n\r\n## 11. 🟡 [Medium] `note-line` 스타일이 `.question-stem`에만 적용, `.option-text`/`.feedback`에는 누락\r\n\r\n### 위치\r\n`www/style.css`\r\n\r\n```css\r\n.question-stem .note-line { ... }\r\n/* .option-text .note-line, .feedback .note-line 없음 */\r\n```\r\n\r\n### 문제\r\n`normalizeQuestionBlock()`(branch 신규 함수)은 `type: \"note\"` 블록을 어디서든(문제 본문, 선택지, 해설) 생성할 수 있도록 일반화되어 있으나, CSS는 `.question-stem` 컨텍스트만 스타일링합니다. 선택지나 해설에 `note` 블록이 포함된 문제 데이터가 들어오면 일반 텍스트와 시각적으로 구분되지 않습니다.\r\n\r\n### 권장 조치\r\n셀렉터를 `.question-stem .note-line, .option-text .note-line, .feedback .note-line`으로 확장.\r\n\r\n---\r\n\r\n## 12. 🟡 [Medium] 고아(orphan) CSS — 제거된 \"긴 지문 접기/펼치기\" 기능의 잔존 스타일\r\n\r\n### 위치\r\n`www/style.css` (다음 클래스 약 40줄, 모바일 미디어쿼리 내 중복分 포함)\r\n```css\r\n.question-stem.stem-collapsed { ... }\r\n.stem-toggle-btn { ... }\r\n.stem-toggle-btn:hover { ... }\r\n.stem-toggle-btn .stem-toggle-icon { ... }\r\n.stem-toggle-btn.expanded .stem-toggle-icon { ... }\r\n```\r\n\r\n`www/script.js`:\r\n```js\r\n// 긴 지문 접기/펼치기 기능 제거됨\r\ndocument.querySelector(\".stem-toggle-btn\")?.remove();\r\nquestionStem.classList.remove(\"stem-collapsed\");\r\n```\r\n\r\n### 문제\r\nJS에서는 기능이 의도적으로 제거되었음을 주석으로 명시했지만, 해당 클래스를 생성하는 코드가 없으므로 CSS 규칙은 영구적으로 매치되지 않는 죽은 코드입니다.\r\n\r\n### 권장 조치\r\nCSS에서 위 셀렉터 전부 삭제 (모바일 미디어쿼리 내 중복 정의 포함).\r\n\r\n---\r\n\r\n## 13. 🟡 [Medium] 오답 노트 필터 버튼 — 인라인 스타일로 구현되어 다크모드/반응형 미적용\r\n\r\n### 위치\r\n`www/script.js` `renderWrongNote()` 내부 `createFilterBtn`\r\n\r\n```js\r\nconst btn = document.createElement(\"button\");\r\nbtn.type = \"button\";\r\nbtn.textContent = text;\r\nbtn.style.whiteSpace = \"nowrap\";\r\nbtn.style.padding = \"8px 16px\";\r\nbtn.style.borderRadius = \"20px\";\r\nbtn.style.minHeight = \"36px\";\r\nif (wrongNoteFilter === value) {\r\n  btn.className = \"primary\";\r\n} else {\r\n  btn.className = \"subtle\";\r\n  btn.style.border = \"1px solid var(--line)\";\r\n}\r\n```\r\n\r\n### 문제\r\n- `btn.style.border = \"1px solid var(--line)\"`처럼 인라인 스타일에 CSS 변수를 사용하는 것은 동작은 하지만, 반응형 브레이크포인트별 오버라이드(`@media`)가 불가능합니다.\r\n- 클래스 기반 스타일링과 혼용되어 유지보수 시 스타일 출처 파악이 어려움.\r\n\r\n### 권장 조치\r\n`.wrong-note-filter-btn` 클래스를 `style.css`에 정의하고, JS에서는 `btn.classList.add(\"wrong-note-filter-btn\", isActive ? \"active\" : \"\")` 형태로 전환.\r\n\r\n---\r\n\r\n## 14. 🟡 [Medium] `.wrong-note-filters` 가로 스크롤에 시각적 힌트 없음\r\n\r\n### 위치\r\n`www/style.css`\r\n\r\n```css\r\n.wrong-note-filters {\r\n  display: flex;\r\n  gap: 8px;\r\n  overflow-x: auto;\r\n  padding-bottom: 10px;\r\n  margin-bottom: 10px;\r\n  scrollbar-width: none;     /* 스크롤바 숨김 */\r\n}\r\n.wrong-note-filters::-webkit-scrollbar {\r\n  display: none;\r\n}\r\n```\r\n\r\n### 문제\r\n채점 기록(`state.histories`)이 많아질수록(최대 30개) 필터 버튼이 가로로 길게 늘어나지만, 스크롤바를 완전히 숨겨서 \"더 많은 기록이 있다\"는 시각적 신호가 전혀 없습니다. 사용자가 최근 몇 개의 기록만 보고 더 이전 기록이 있다는 것을 인지하지 못할 수 있습니다.\r\n\r\n### 권장 조치\r\n좌우 끝에 `mask-image`/`box-shadow` 페이드 효과를 추가하거나, 좌우 화살표 버튼을 제공.\r\n\r\n---\r\n\r\n## 15. 🟡 [Medium] `resetAnswersFor()` — 답안 초기화 시 히스토리(`state.histories`)는 유지됨 (의도 확인 필요)\r\n\r\n### 위치\r\n`www/script.js`\r\n\r\n```js\r\nfunction resetAnswersFor(mode) {\r\n  const questions = mode === \"random\" ? randomQuestions() : currentSet().questions;\r\n  questions.forEach((question) => {\r\n    delete state.answers[answerKey(question, mode)];\r\n    delete state.answers[legacyAnswerKey(question, mode)];\r\n  });\r\n}\r\n```\r\n\r\n### 확인 필요 사항\r\n\"현재 모드 풀이 초기화\"(`resetBtn`) 시 `state.answers`만 삭제하고 `state.histories`(이전 채점 기록)는 그대로 남습니다. 이는 \"오답 노트\"가 과거 채점 시점의 기록을 보존한다는 점에서 의도된 동작일 수 있으나, 위 **#1 버그**와 결합하면 \"초기화 후에도 오답 노트에 (잘못된) 전체 오답 기록이 계속 누적되어 표시\"되는 결과로 이어집니다. #1을 수정할 때 이 흐름도 함께 재검토 필요.\r\n\r\n---\r\n\r\n## 16. ⚪ [Low] `verify.js`의 `questions.js` 참조 금지 검사 — 메시지 보강 권장\r\n\r\n### 위치\r\n`scripts/verify.js`\r\n\r\n```js\r\nconst html = fs.readFileSync(path.join(root, \"www\", \"index.html\"), \"utf8\");\r\nif (/questions\\.js|csts-questions\\.js/.test(html)) {\r\n  throw new Error(\"www/index.html must not load questions.js or csts-questions.js\");\r\n}\r\n```\r\n\r\n### 의견\r\n이번 브랜치의 데이터 로딩 방식 전환(`<script src=\"./questions.js\">` → `fetch(\"./data/index.json\")`)을 강제하기 위한 좋은 가드입니다. 다만 에러 메시지에 \"데이터는 `data/index.json`을 통해 fetch로 로드해야 합니다\" 같은 **대안 안내**를 추가하면 향후 기여자가 원인을 더 빨리 파악할 수 있습니다.\r\n\r\n---\r\n\r\n## 17. ⚪ [Low] `figureFor()`의 그림 매핑 테이블이 하드코딩되어 데이터 주도(Data-driven)가 아님\r\n\r\n### 위치\r\n`www/script.js` `figureFor(question)`\r\n\r\n```js\r\nfunction figureFor(question) {\r\n  if (question.figure) return question.figure;\r\n  const setId = question.setId || state.setId;\r\n  const sampleCode = String(setId || \"\").split(\"-\").pop();\r\n  const key = `${sampleCode}${question.number}`;\r\n  const figures = {\r\n    A23: \"figures/A23.png\",\r\n    B23: \"figures/B23.png\",\r\n    C23: \"figures/C23.png\",\r\n    C24: \"figures/C24.png\",\r\n    C31: \"figures/C31.png\",\r\n    C32: \"figures/C32.png\",\r\n  };\r\n  return figures[key] || \"\";\r\n}\r\n```\r\n\r\n### 의견\r\n`question.figure` 필드가 이미 존재하여 우선 사용되므로, 이 하드코딩 테이블은 `figure` 필드가 비어 있는 레거시 문제들을 위한 fallback으로 보입니다. `audit-phase3-content.js`에서 그림이 필요한 문제 ID 목록(`imageRequired`)을 관리하고 있으므로, 장기적으로는 모든 문제 JSON에 `figure` 필드를 채워 이 하드코딩 테이블을 제거하는 것이 데이터 일관성 측면에서 바람직합니다.\r\n\r\n---\r\n\r\n## 18. ⚪ [Low] `historyAnswers` 키 필터링 방식 자체의 설계 재검토 권장 (#1과 연계)\r\n\r\n`#1`의 직접적 수정과 별개로, \"현재 세트/모드의 답안만 골라서 히스토리에 저장\"하는 방식 자체가 `state.answers`의 **전체 키를 문자열 패턴 매칭으로 필터링**하는 구조입니다. 이는 `answerKey()`의 포맷이 바뀔 때마다(이번처럼) 조용히 깨질 수 있는 취약한 설계입니다.\r\n\r\n장기적으로는 \"현재 세트/모드에 속한 질문 목록(`currentSet().questions` 또는 `randomQuestions()`)을 순회하며 `answerKey(question, mode)`로 직접 키를 계산\"하는 방식으로 전환하면, 키 포맷이 바뀌어도 히스토리 저장 로직이 깨지지 않습니다 (#1의 제안과 동일).\r\n\r\n---\r\n\r\n## 부록: 파일별 변경 통계 (main 대비)\r\n\r\n| 파일 | 변경 | 비고 |\r\n|---|---|---|\r\n| `www/script.js` | 3333 → 3544줄 (+211) | 데이터 로딩 방식 전환, 오답 노트, 타이머, 그림 키 수정 등 |\r\n| `www/style.css` | 변경 ~30곳 | 설정 패널, note-line, multi-answer-badge, 테이블 등 신규 스타일 |\r\n| `www/index.html` | `<script src=\"./questions.js\">`, `<script src=\"./csts-questions.js\">` 제거 | 신규 fetch 기반 로딩과 일치 |\r\n\r\n---\r\n\r\n## 우선순위 권장 작업 순서\r\n\r\n1. **#1 오답 노트 키 불일치** — 핵심 기능 정상화 (최우선)\r\n2. **#7, #8, #9** — 모바일 UI 클리핑/스크롤 문제 (사용성 직접 영향)\r\n3. **#5** — IndexedDB 복원 경쟁 조건 (재현 어렵지만 영향 큼)\r\n4. **#4** — 랜덤 모드 40문항 가드 (현재는 안전하지만 잠재적 회귀 방지)\r\n5. **#10, #11** — 다크모드/스타일 일관성\r\n6. **#6, #12, #13, #14, #15, #16~18** — 정리성 작업, 여유 있을 때 진행\r\n\n</USER_REQUEST>\n<ADDITIONAL_METADATA>\nThe current local time is: 2026-06-14T13:54:10+09:00.\n\nThe user has mentioned some items in the form @[ITEM]. Here is extra information about the items that were mentioned by the user, in the order that they appear:\n\n/goal is a [Slash Command]:\nThe user has marked this task with /goal, indicating that this task is intended to run for a long time without user input, e.g. overnight. You should be extra thorough and only stop when you are confident the goal has been completely fulfilled. The system will force you to continue execution, prompting you to audit your work until completion.\n</ADDITIONAL_METADATA>"

"<USER_REQUEST>\n로컬 서버 열어봐\n</USER_REQUEST>\n<ADDITIONAL_METADATA>\nThe current local time is: 2026-06-14T14:25:02+09:00.\n</ADDITIONAL_METADATA>"

"<USER_REQUEST>\n/goal # `codex/fix-issues-in-phase-4` 브랜치 코드 리뷰 리포트\r\n\r\n대상 저장소: `WindowHyun/ISTQB`\r\n대상 브랜치: `codex/fix-issues-in-phase-4`\r\n비교 기준: `main`\r\n대상 파일: `www/script.js`, `www/style.css`, `www/index.html`\r\n\r\n---\r\n\r\n## 0. 요약\r\n\r\n| 등급 | 개수 | 설명 |\r\n|---|---|---|\r\n| 🔴 Critical | 5건 | 핵심 기능이 동작하지 않거나 데이터가 손상될 수 있는 버그 |\r\n| 🟠 High | 4건 | 특정 조건에서 화면/동작이 명백히 잘못되는 문제 |\r\n| 🟡 Medium | 6건 | UX 저하, 접근성/다크모드 누락, 잠재적 회귀 |\r\n| ⚪ Low | 3건 | 코드 정리, 유지보수성 |\r\n\r\n가장 심각한 항목은 **#1 오답 노트 기능 자체가 동작하지 않는 버그**입니다. 이번 브랜치의 핵심 신규 기능(README에 명시된 \"오답 다시풀기\" / \"오답 노트\")이 채점 결과를 전혀 반영하지 못합니다.\r\n\r\n---\r\n\r\n## 1. 🔴 [Critical] 오답 노트 기능이 항상 \"전부 오답\"으로 표시됨\r\n\r\n### 위치\r\n`www/script.js` — 채점(`gradeBtn` 클릭) 핸들러 내부, `historyAnswers` 생성 부분.\r\n\r\n### 코드\r\n\r\n```js\r\n// 채점 시 히스토리에 저장할 답안을 추출하는 부분\r\nconst historyAnswers = {};\r\nObject.keys(state.answers).forEach(key => {\r\n  if (key.includes(`-${targetMode}-`)) {\r\n     historyAnswers[key] = state.answers[key];\r\n  }\r\n});\r\n\r\nstate.histories.push({\r\n  id: timestamp.toString(),\r\n  timestamp,\r\n  mode: state.mode,\r\n  setId: targetSetId,\r\n  answers: historyAnswers,\r\n  randomRefs: state.mode === \"random\" ? [...state.randomRefs] : null\r\n});\r\n```\r\n\r\n### 문제\r\n\r\n이번 브랜치에서 `answerKey()`가 다음과 같이 변경되었습니다 (Phase 4의 핵심 개선 중 하나):\r\n\r\n```js\r\nfunction answerKey(question, mode = answerMode()) {\r\n  if (question.id) return `${question.id}-${mode}`;   // 신규 포맷: \"ISTQB-FL-V4-A-001-exam\"\r\n  return legacyAnswerKey(question, mode);\r\n}\r\n\r\nfunction legacyAnswerKey(question, mode = answerMode()) {\r\n  const setId = question.legacySetId || question.setId || state.setId;\r\n  return `${setId}-${mode}-${question.number}`;       // 구 포맷: \"ISTQB-FL-V4-A-exam-1\"\r\n}\r\n```\r\n\r\n실제 데이터(`www/data/istqb/sample-a.json` 등)의 모든 문제에는 `id` 필드가 존재합니다 (`\"id\": \"ISTQB-FL-V4-A-001\"`). 따라서 `chooseOption()` → `saveAnswers()`로 저장되는 실제 키는 항상 **신규 포맷** `\"ISTQB-FL-V4-A-001-exam\"` 형태입니다.\r\n\r\n그런데 히스토리 생성 시 필터는 `key.includes(`-${targetMode}-`)`, 즉 **`\"-exam-\"`(앞뒤로 하이픈)이 포함되어야** 함을 요구합니다.\r\n\r\n```text\r\n신규 포맷: \"ISTQB-FL-V4-A-001-exam\"  → \"-exam-\" 포함?  ❌ (끝이 \"-exam\"으로 끝나서 뒤에 \"-\"가 없음)\r\n구 포맷:   \"ISTQB-FL-V4-A-exam-1\"    → \"-exam-\" 포함?  ✅\r\n```\r\n\r\n결과적으로 **`historyAnswers`는 항상 빈 객체 `{}`** 가 됩니다.\r\n\r\n이후 오답 노트를 그릴 때:\r\n\r\n```js\r\nfunction historyWrongNoteItems(history) {\r\n  ...\r\n  questions.forEach((question) => {\r\n     const ansKey = `${question.setId || history.setId}-${history.mode}-${question.number}`; // 구 포맷 키로 조회\r\n     const selected = history.answers[ansKey] || [];   // 항상 [] (history.answers === {})\r\n     const isCor =\r\n       question.type === \"short_answer\"\r\n         ? normalizeTextAnswer(selected[0]) === normalizeTextAnswer(question.answer[0])\r\n         : sameChoices(selected, question.answer);      // sameChoices([], [...]) → 항상 false\r\n\r\n     if (!isCor) {\r\n       items.push({ ... });   // 모든 문제가 여기로 들어감\r\n     }\r\n  });\r\n}\r\n```\r\n\r\n`selected`가 항상 빈 배열이므로 `isCor`는 모든 문제에 대해 `false`가 되고, **채점 결과와 무관하게 세트 내 모든 문제(예: 40문항)가 오답 노트에 표시**됩니다.\r\n\r\n### 영향\r\n- README에서 강조한 핵심 기능(\"오답 다시풀기를 누르기 전까지 기존 오답 기록 보호\", \"오답 노트\")이 사실상 무의미해짐.\r\n- `오답 다시풀기`(`retryWrongBtn`)는 `missedExamQuestions()`를 사용하므로 별도 경로이고 정상 동작하지만, **오답 노트 모달에 표시되는 목록과 실제 채점 결과가 일치하지 않음** → 사용자에게 혼란을 줌.\r\n\r\n### 수정 방향 (제안)\r\n`historyAnswers` 필터와 `historyWrongNoteItems`의 조회 키를 모두 `answerKey(question, mode)` 기준(신규 포맷)으로 통일해야 합니다.\r\n\r\n```js\r\n// 필터: question 객체 기준으로 직접 키를 계산\r\nconst targetQuestions =\r\n  state.mode === \"random\" ? randomQuestions() : currentSet().questions;\r\n\r\nconst historyAnswers = {};\r\ntargetQuestions.forEach((question) => {\r\n  const key = answerKey(question, targetMode);\r\n  if (state.answers[key]) historyAnswers[key] = state.answers[key];\r\n});\r\n```\r\n\r\n```js\r\n// historyWrongNoteItems 조회도 동일하게\r\nconst ansKey = answerKey(question, history.mode === \"random\" ? \"random\" : \"exam\");\r\nconst selected = history.answers[ansKey] || [];\r\n```\r\n\r\n---\r\n\r\n## 2. 🔴 [Critical] `figureFor()` 키 생성 버그 — 그림이 표시되지 않음\r\n\r\n### 위치\r\n`www/script.js` `figureFor(question)`\r\n\r\n### 코드 (main vs branch)\r\n\r\n```js\r\n// main (버그)\r\nconst setId = question.setId || state.setId;\r\nconst key = `${setId}${question.number}`;   // \"ISTQB-FL-V4-A23\" → 매핑 테이블 키와 불일치\r\n\r\n// branch (수정 완료)\r\nconst sampleCode = String(setId || \"\").split(\"-\").pop();\r\nconst key = `${sampleCode}${question.number}`;  // \"A23\" → 매핑 테이블과 일치\r\n```\r\n\r\n### 상태\r\n✅ **이 브랜치에서 이미 수정됨.** (참고용으로 기록 — 회귀 방지를 위해 테스트 케이스 추가를 권장)\r\n\r\n---\r\n\r\n## 3. 🔴 [Critical] 타이머 백그라운드 복귀 시 폭증 — `elapsedSeconds` 방식으로 수정됨\r\n\r\n### 위치\r\n`www/script.js` `updateTimer()`, `state.elapsedSeconds` / `state.lastTick`\r\n\r\n### 상태\r\n✅ **이 브랜치에서 절대시각(`startedAt`) 방식 → 누적시간(`elapsedSeconds`) + `lastTick` 델타 방식으로 수정됨.**\r\n\r\n다만 아래 **#7 IndexedDB 복원 경쟁 조건**과 함께 검토 필요 (관련 상태가 비동기 복원 대상에 포함됨).\r\n\r\n---\r\n\r\n## 4. 🔴 [Critical] `randomQuestions()` — 전체 문항 수가 40개 미만이면 매 렌더마다 무한 재셔플\r\n\r\n### 위치\r\n`www/script.js`\r\n\r\n```js\r\nfunction generateRandomRefs() {\r\n  state.randomRefs = shuffle(allQuestions())\r\n    .slice(0, 40)\r\n    .map((question) => ({ setId: question.setId, number: question.number }));\r\n}\r\n\r\nfunction randomQuestions() {\r\n  if (state.randomRefs.length !== 40) generateRandomRefs();  // ⚠️\r\n  return state.randomRefs\r\n    .map((ref) => { ... })\r\n    .filter(Boolean);\r\n}\r\n```\r\n\r\n### 문제\r\n`allQuestions()`가 반환하는 전체 문항 수가 40개 미만인 제품/세트 구성이라면:\r\n\r\n- `shuffle(allQuestions()).slice(0, 40)`은 **40개보다 적은 배열**을 반환합니다.\r\n- `state.randomRefs.length !== 40` 조건이 **항상 true**가 되어,\r\n- `randomQuestions()`가 호출될 때마다(즉 `render()`가 호출될 때마다) `generateRandomRefs()`가 다시 실행됩니다.\r\n\r\n결과적으로 **랜덤 모드에 진입한 직후부터 매 렌더링마다 문제 순서가 무작위로 재섹션**됩니다. 사용자가 답을 선택해 `render()`가 다시 호출되는 순간 `currentQuestions()`(=`randomQuestions()`)의 순서/구성이 바뀌면서:\r\n- 현재 보고 있던 문제가 다른 문제로 바뀌거나,\r\n- `state.index`가 가리키는 문제가 매번 달라짐.\r\n\r\n### 현재 안전한 이유\r\n현재 ISTQB(186문항)·CSTS(각 세트 수십 문항 합산) 데이터셋은 합계가 40을 넘기 때문에 **지금 당장은 발생하지 않음**. 그러나:\r\n- 향후 세트를 줄이거나, 특정 제품에 문항이 적은 카테고리를 추가하면 즉시 재현되는 **잠재적 타임 폭탄(time bomb)**.\r\n- 전체 문항 수가 정확히 40개 이상인지 검증하는 가드가 코드 어디에도 없음.\r\n\r\n### 수정 방향 (제안)\r\n```js\r\nfunction randomQuestions() {\r\n  const targetCount = Math.min(40, allQuestions().length);\r\n  if (state.randomRefs.length !== targetCount || state.randomRefs.length === 0) {\r\n    generateRandomRefs();\r\n  }\r\n  ...\r\n}\r\n```\r\n혹은 최소한 `allQuestions().length < 40`일 때 경고 로그 + 가능한 전체 문항 수로 대체.\r\n\r\n---\r\n\r\n## 5. 🔴 [Critical] `restorePersistentSnapshot()`의 비동기 경쟁 조건 (Race Condition)\r\n\r\n### 위치\r\n`www/script.js`\r\n\r\n```js\r\n// 초기화 마지막 부분\r\nrenderExamSelect();\r\nrenderMode();\r\nrender();\r\nrestorePersistentSnapshot();   // await 하지 않음 (fire-and-forget)\r\n```\r\n\r\n```js\r\nasync function restorePersistentSnapshot() {\r\n  let snapshot = null;\r\n  let hasUiState = false;\r\n  try {\r\n    snapshot = JSON.parse(localStorage.getItem(persistenceKey()));\r\n    hasUiState = Boolean(localStorage.getItem(uiStorageKey()));   // ← 이 시점에 동기적으로 평가\r\n  } catch { snapshot = null; }\r\n\r\n  const db = await openPersistenceDb();          // ← IndexedDB open은 비동기, 수~수십ms 소요 가능\r\n  if (db) {\r\n    try {\r\n      snapshot = await new Promise((resolve) => { ... });\r\n    } catch { ... }\r\n  }\r\n  if (!snapshot || typeof snapshot !== \"object\") return;\r\n  if (snapshot.answers && Object.keys(state.answers).length === 0) {\r\n    state.answers = sanitizeAnswerState(snapshot.answers);\r\n  }\r\n  if (snapshot.uiState && !hasUiState) {\r\n    applyBackupSnapshot({ answers: state.answers, uiState: snapshot.uiState });  // ← 전체 state 덮어씀\r\n  }\r\n  renderExamSelect();\r\n  renderMode();\r\n  render();\r\n}\r\n```\r\n\r\n### 문제\r\n1. `hasUiState`는 **함수 진입 시점**(IndexedDB 열기 이전)의 `localStorage` 상태로 결정됩니다.\r\n2. 만약 사용자가 **앱 최초 진입 직후 즉시** (IndexedDB가 열리기 전, 수~수십ms 사이) 옵션을 클릭하면:\r\n   - `chooseOption()` → `saveAnswers()` + `saveUiState()` 실행 → `localStorage`에 `uiState`/`answers`가 기록됨.\r\n   - 그러나 `hasUiState` 변수는 이미 `false`로 캡처되어 있으므로, 이후 IndexedDB에서 가져온 **오래된 snapshot.uiState로 사용자의 첫 클릭 상태가 덮어써짐(`applyBackupSnapshot`)**.\r\n3. `state.answers` 조건은 `Object.keys(state.answers).length === 0`으로 체크하므로 답안 자체는 비교적 안전하지만, **`state.mode`, `state.index`, `examGraded`, `reviewRetake` 등 UI 상태 전체가 스냅샷으로 되돌아갈 수 있음** — 예: 사용자가 \"시험 모드\"로 전환했는데 다시 \"연습 모드\"로 되돌아가는 식의 깜빡임/되돌림 현상.\r\n\r\n### 영향\r\n- 실제 발생 빈도는 낮지만(타이밍 윈도우가 매우 짧음), 느린 기기나 IndexedDB 초기화가 지연되는 환경(특히 Android WebView)에서는 재현 가능성이 더 높음.\r\n- 디버깅이 매우 어려운 \"가끔 처음 클릭이 무시되는 것처럼 보이는\" 버그로 보고될 가능성.\r\n\r\n### 수정 방향 (제안)\r\n- 앱 부팅 시 `restorePersistentSnapshot()`을 **`render()` 이전에 `await`** 하거나,\r\n- `hasUiState`를 `restorePersistentSnapshot` 완료 시점에 다시 평가(재확인)하거나,\r\n- IndexedDB 복원이 끝나기 전까지 사용자 입력을 막는 짧은 로딩 상태를 두는 것을 고려.\r\n\r\n---\r\n\r\n## 6. 🟠 [High] CSTS \"미리보기\" 페이지(`#cstsPage`) — 죽은 코드 (Dead Code), 회귀는 아니지만 정리 필요\r\n\r\n### 위치\r\n`www/index.html` `<section id=\"cstsPage\" ...>`, `www/script.js`의 `currentCstsSet`, `renderCstsSelect`, `renderCstsFigure`, `renderCstsOptions`, `renderCstsPage`, 그리고 `cstsSetSelect`/`cstsPrevBtn`/`cstsNextBtn` 이벤트 리스너.\r\n\r\n### 문제\r\n`openCstsApp()`은 다음과 같이 구현되어 있습니다:\r\n\r\n```js\r\nfunction openCstsApp() {\r\n  startProduct(\"csts\");\r\n  showActiveProductApp();   // appShell(메인 퀴즈 UI)만 보여줌\r\n  questionTitle?.focus?.();\r\n}\r\n\r\nfunction showActiveProductApp() {\r\n  productGate?.classList.add(\"is-product-hidden\");\r\n  productGate?.setAttribute(\"hidden\", \"\");\r\n  cstsPage?.classList.add(\"is-product-hidden\");   // cstsPage는 항상 숨김\r\n  cstsPage?.setAttribute(\"hidden\", \"\");\r\n  appShell?.classList.remove(\"is-product-hidden\");\r\n}\r\n```\r\n\r\n즉 CSTS 버튼을 눌러도 **`#cstsPage`(전용 미리보기 UI)는 절대 표시되지 않고**, ISTQB와 동일한 `appShell`이 CSTS 데이터로 렌더링됩니다. (이 동작은 `main`에서도 동일했으므로 이번 브랜치의 회귀는 아닙니다.)\r\n\r\n그 결과:\r\n- `renderCstsPage()`, `renderCstsSelect()`, `renderCstsFigure()`, `renderCstsOptions()`, `currentCstsSet()` — 약 100여 줄의 함수와 `cstsState`, 그리고 `index.html`의 `#cstsPage` 섹션 전체(태그, 버튼, select)가 **호출되지 않는 죽은 코드**입니다.\r\n- `cstsSetSelect`, `cstsPrevBtn`, `cstsNextBtn`에 등록된 이벤트 리스너도 절대 발생하지 않음.\r\n\r\n### 영향\r\n- 코드베이스 크기 증가, 신규 기여자의 혼란 가능성(\"이 페이지는 언제 쓰이지?\").\r\n- 향후 데이터 구조가 바뀌면 이 죽은 코드도 같이 수정해야 한다고 오인하여 불필요한 작업이 발생할 수 있음.\r\n\r\n### 권장 조치\r\n- `#cstsPage` 관련 HTML/CSS/JS를 완전히 제거하거나,\r\n- 혹은 \"간단 미리보기 모드\"로 의도적으로 부활시켜 실제로 연결할지 결정 필요 (제품 결정 사항).\r\n\r\n---\r\n\r\n## 7. 🟠 [High] 설정 패널(`.settings-content`)이 모바일 사이드바에서 잘림 + 외부 클릭 닫기 없음\r\n\r\n### 위치\r\n`www/style.css`, `www/script.js` (`settingsPanelToggleBtn` 리스너)\r\n\r\n### 코드\r\n\r\n```css\r\n/* branch에서 변경됨: static → absolute */\r\n.settings-content {\r\n  position: absolute;\r\n  top: calc(100% + 8px);\r\n  left: 0;\r\n  right: 0;\r\n  z-index: 80;\r\n  max-height: min(70vh, 560px);\r\n  overflow-y: auto;\r\n  ...\r\n}\r\n```\r\n\r\n```css\r\n/* 태블릿 세로 이하(≤900px): 펼쳐진 사이드바 */\r\n.sidebar:not(.mobile-collapsed) {\r\n  max-height: min(58vh, 560px);\r\n  overflow-y: auto;   /* ← absolute 자식이 여기서 클리핑됨 */\r\n}\r\n```\r\n\r\n```js\r\nsettingsPanelToggleBtn.addEventListener(\"click\", () => {\r\n  const isOpen = settingsPanelToggleBtn.getAttribute(\"aria-expanded\") === \"true\";\r\n  settingsPanelToggleBtn.setAttribute(\"aria-expanded\", String(!isOpen));\r\n  settingsPanel.hidden = isOpen;\r\n});\r\n// 패널 바깥 클릭 시 닫는 핸들러 없음\r\n```\r\n\r\n### 문제\r\n1. `.settings-content`는 `position: absolute`이고 부모 체인에 `overflow-y: auto`인 `.sidebar:not(.mobile-collapsed)`가 있어, 패널이 사이드바의 `max-height`(58vh)를 넘으면 **잘려서 일부 버튼(초기화, 기록 관리 등)에 접근할 수 없음**.\r\n2. 패널 외부를 클릭/탭해도 닫히지 않아, 모바일에서 열어둔 패널이 화면을 계속 가림.\r\n\r\n### 권장 조치\r\n- 모바일 브레이크포인트에서는 `.settings-content`를 모달(`.modal-backdrop` 패턴)로 전환하거나, `position: fixed` + 자체 스크롤 컨테이너로 변경.\r\n- `document.addEventListener(\"click\", ...)`로 외부 클릭 시 `settingsPanel.hidden = true` 처리 추가.\r\n\r\n---\r\n\r\n## 8. 🟠 [High] `.figure-zoom-btn`이 가로 스크롤 컨테이너에서 잘림\r\n\r\n### 위치\r\n`www/style.css`\r\n\r\n```css\r\n.question-figure {\r\n  position: relative;\r\n  margin: 4px 0 8px;\r\n  overflow-x: auto;     /* ← absolute 자식을 클리핑 */\r\n}\r\n\r\n.figure-zoom-btn {\r\n  position: absolute;\r\n  right: 10px;\r\n  bottom: 10px;\r\n  ...\r\n}\r\n```\r\n\r\n### 문제\r\n`.question-figure img`는 `max-width: min(100%, 760px)`이라 컨테이너보다 클 수 있고, 이 경우 `.question-figure`에 가로 스크롤이 생깁니다. `.figure-zoom-btn`은 `absolute`로 컨테이너 우하단에 고정되도록 의도되었지만, `overflow-x: auto`인 부모 안에서는 **스크롤 가능한 콘텐츠 영역 기준으로 위치가 계산**되어 화면에 보이지 않거나 스크롤해야만 보이는 위치에 배치될 수 있습니다.\r\n\r\n### 권장 조치\r\n- 확대 버튼을 `.question-figure` 바깥의 별도 wrapper(`position: relative`, `overflow: visible`)에 `position: sticky` 또는 `fixed` 형태로 배치.\r\n- 또는 이미지 자체에 `cursor: zoom-in`만 유지하고, 확대 버튼은 이미지 위에 오버레이가 아닌 이미지 아래 별도 영역에 고정 배치.\r\n\r\n---\r\n\r\n## 9. 🟠 [High] 문제 번호 네비게이션(`#questionNav`)에 `scrollIntoView` 없음\r\n\r\n### 위치\r\n`www/script.js` `renderNav(questions)`\r\n\r\n```js\r\nfunction renderNav(questions) {\r\n  questionNav.replaceChildren();\r\n  const fragment = document.createDocumentFragment();\r\n  questions.forEach((question, index) => {\r\n    const button = document.createElement(\"button\");\r\n    ...\r\n    button.classList.toggle(\"current\", index === state.index);\r\n    ...\r\n    fragment.appendChild(button);\r\n  });\r\n  questionNav.appendChild(fragment);\r\n  // 현재 문제 버튼을 보이는 영역으로 스크롤하는 로직 없음\r\n}\r\n```\r\n\r\n### 문제\r\n`.question-nav`는 고정된 그리드(`repeat(20, minmax(34px, 1fr))` 등)이며, 컨테이너 자체는 스크롤 가능한 영역(`.sidebar` 등) 안에 위치합니다. \"이전/다음\" 버튼이나 키보드 탐색으로 `state.index`가 바뀌어도, 현재 선택된 `.current` 버튼이 화면 밖에 있으면 **사용자가 스크롤해야만 현재 위치를 확인**할 수 있습니다.\r\n\r\n### 권장 조치\r\n```js\r\nconst currentBtn = questionNav.querySelector(\".current\");\r\ncurrentBtn?.scrollIntoView({ block: \"nearest\", inline: \"nearest\" });\r\n```\r\n`renderNav()` 끝부분에 추가.\r\n\r\n---\r\n\r\n## 10. 🟡 [Medium] 신규 컴포넌트들이 다크모드(`prefers-color-scheme: dark`)를 지원하지 않음\r\n\r\n### 위치\r\n`www/style.css`\r\n\r\n| 컴포넌트 | 하드코딩 색상 | 비고 |\r\n|---|---|---|\r\n| `.question-stem .note-line` | `#b8cbc5`(테두리), `#f8fbfa`(배경) | 다크 배경에서 흰 박스로 표시 |\r\n| `.multi-answer-badge` | `#f0c96a`, `linear-gradient(90deg, #fff9e6, #fffdf5)`, `#7a5200` | 다크모드에서 가독성 저하 |\r\n| `.data-table`, `.table-scroll`, `.table-hint` | `#c8d7d2`, `#fbfdfc`, `#eef7f4`, `#dce7e3` | 흰 테이블이 다크 배경 위에 튀어 보임 |\r\n\r\n### 문제\r\n`@media (prefers-color-scheme: dark) { ... }` 블록이 존재하지만, 위 3개 컴포넌트는 모두 **이번 브랜치에서 신규/변경된 것**이며 다크모드 오버라이드 목록에 포함되어 있지 않습니다.\r\n\r\n### 권장 조치\r\n다크모드 미디어 쿼리 안에 다음과 같은 오버라이드 추가:\r\n```css\r\n@media (prefers-color-scheme: dark) {\r\n  .question-stem .note-line {\r\n    border-left-color: var(--border);\r\n    background: var(--secondary);\r\n    color: var(--muted-foreground);\r\n  }\r\n  .multi-answer-badge {\r\n    border-color: rgba(240, 187, 104, 0.4);\r\n    background: rgba(240, 187, 104, 0.12);\r\n    color: var(--warning);\r\n  }\r\n  .table-scroll, .data-table, .data-table th {\r\n    border-color: var(--border);\r\n    background: var(--card);\r\n  }\r\n}\r\n```\r\n\r\n---\r\n\r\n## 11. 🟡 [Medium] `note-line` 스타일이 `.question-stem`에만 적용, `.option-text`/`.feedback`에는 누락\r\n\r\n### 위치\r\n`www/style.css`\r\n\r\n```css\r\n.question-stem .note-line { ... }\r\n/* .option-text .note-line, .feedback .note-line 없음 */\r\n```\r\n\r\n### 문제\r\n`normalizeQuestionBlock()`(branch 신규 함수)은 `type: \"note\"` 블록을 어디서든(문제 본문, 선택지, 해설) 생성할 수 있도록 일반화되어 있으나, CSS는 `.question-stem` 컨텍스트만 스타일링합니다. 선택지나 해설에 `note` 블록이 포함된 문제 데이터가 들어오면 일반 텍스트와 시각적으로 구분되지 않습니다.\r\n\r\n### 권장 조치\r\n셀렉터를 `.question-stem .note-line, .option-text .note-line, .feedback .note-line`으로 확장.\r\n\r\n---\r\n\r\n## 12. 🟡 [Medium] 고아(orphan) CSS — 제거된 \"긴 지문 접기/펼치기\" 기능의 잔존 스타일\r\n\r\n### 위치\r\n`www/style.css` (다음 클래스 약 40줄, 모바일 미디어쿼리 내 중복分 포함)\r\n```css\r\n.question-stem.stem-collapsed { ... }\r\n.stem-toggle-btn { ... }\r\n.stem-toggle-btn:hover { ... }\r\n.stem-toggle-btn .stem-toggle-icon { ... }\r\n.stem-toggle-btn.expanded .stem-toggle-icon { ... }\r\n```\r\n\r\n`www/script.js`:\r\n```js\r\n// 긴 지문 접기/펼치기 기능 제거됨\r\ndocument.querySelector(\".stem-toggle-btn\")?.remove();\r\nquestionStem.classList.remove(\"stem-collapsed\");\r\n```\r\n\r\n### 문제\r\nJS에서는 기능이 의도적으로 제거되었음을 주석으로 명시했지만, 해당 클래스를 생성하는 코드가 없으므로 CSS 규칙은 영구적으로 매치되지 않는 죽은 코드입니다.\r\n\r\n### 권장 조치\r\nCSS에서 위 셀렉터 전부 삭제 (모바일 미디어쿼리 내 중복 정의 포함).\r\n\r\n---\r\n\r\n## 13. 🟡 [Medium] 오답 노트 필터 버튼 — 인라인 스타일로 구현되어 다크모드/반응형 미적용\r\n\r\n### 위치\r\n`www/script.js` `renderWrongNote()` 내부 `createFilterBtn`\r\n\r\n```js\r\nconst btn = document.createElement(\"button\");\r\nbtn.type = \"button\";\r\nbtn.textContent = text;\r\nbtn.style.whiteSpace = \"nowrap\";\r\nbtn.style.padding = \"8px 16px\";\r\nbtn.style.borderRadius = \"20px\";\r\nbtn.style.minHeight = \"36px\";\r\nif (wrongNoteFilter === value) {\r\n  btn.className = \"primary\";\r\n} else {\r\n  btn.className = \"subtle\";\r\n  btn.style.border = \"1px solid var(--line)\";\r\n}\r\n```\r\n\r\n### 문제\r\n- `btn.style.border = \"1px solid var(--line)\"`처럼 인라인 스타일에 CSS 변수를 사용하는 것은 동작은 하지만, 반응형 브레이크포인트별 오버라이드(`@media`)가 불가능합니다.\r\n- 클래스 기반 스타일링과 혼용되어 유지보수 시 스타일 출처 파악이 어려움.\r\n\r\n### 권장 조치\r\n`.wrong-note-filter-btn` 클래스를 `style.css`에 정의하고, JS에서는 `btn.classList.add(\"wrong-note-filter-btn\", isActive ? \"active\" : \"\")` 형태로 전환.\r\n\r\n---\r\n\r\n## 14. 🟡 [Medium] `.wrong-note-filters` 가로 스크롤에 시각적 힌트 없음\r\n\r\n### 위치\r\n`www/style.css`\r\n\r\n```css\r\n.wrong-note-filters {\r\n  display: flex;\r\n  gap: 8px;\r\n  overflow-x: auto;\r\n  padding-bottom: 10px;\r\n  margin-bottom: 10px;\r\n  scrollbar-width: none;     /* 스크롤바 숨김 */\r\n}\r\n.wrong-note-filters::-webkit-scrollbar {\r\n  display: none;\r\n}\r\n```\r\n\r\n### 문제\r\n채점 기록(`state.histories`)이 많아질수록(최대 30개) 필터 버튼이 가로로 길게 늘어나지만, 스크롤바를 완전히 숨겨서 \"더 많은 기록이 있다\"는 시각적 신호가 전혀 없습니다. 사용자가 최근 몇 개의 기록만 보고 더 이전 기록이 있다는 것을 인지하지 못할 수 있습니다.\r\n\r\n### 권장 조치\r\n좌우 끝에 `mask-image`/`box-shadow` 페이드 효과를 추가하거나, 좌우 화살표 버튼을 제공.\r\n\r\n---\r\n\r\n## 15. 🟡 [Medium] `resetAnswersFor()` — 답안 초기화 시 히스토리(`state.histories`)는 유지됨 (의도 확인 필요)\r\n\r\n### 위치\r\n`www/script.js`\r\n\r\n```js\r\nfunction resetAnswersFor(mode) {\r\n  const questions = mode === \"random\" ? randomQuestions() : currentSet().questions;\r\n  questions.forEach((question) => {\r\n    delete state.answers[answerKey(question, mode)];\r\n    delete state.answers[legacyAnswerKey(question, mode)];\r\n  });\r\n}\r\n```\r\n\r\n### 확인 필요 사항\r\n\"현재 모드 풀이 초기화\"(`resetBtn`) 시 `state.answers`만 삭제하고 `state.histories`(이전 채점 기록)는 그대로 남습니다. 이는 \"오답 노트\"가 과거 채점 시점의 기록을 보존한다는 점에서 의도된 동작일 수 있으나, 위 **#1 버그**와 결합하면 \"초기화 후에도 오답 노트에 (잘못된) 전체 오답 기록이 계속 누적되어 표시\"되는 결과로 이어집니다. #1을 수정할 때 이 흐름도 함께 재검토 필요.\r\n\r\n---\r\n\r\n## 16. ⚪ [Low] `verify.js`의 `questions.js` 참조 금지 검사 — 메시지 보강 권장\r\n\r\n### 위치\r\n`scripts/verify.js`\r\n\r\n```js\r\nconst html = fs.readFileSync(path.join(root, \"www\", \"index.html\"), \"utf8\");\r\nif (/questions\\.js|csts-questions\\.js/.test(html)) {\r\n  throw new Error(\"www/index.html must not load questions.js or csts-questions.js\");\r\n}\r\n```\r\n\r\n### 의견\r\n이번 브랜치의 데이터 로딩 방식 전환(`<script src=\"./questions.js\">` → `fetch(\"./data/index.json\")`)을 강제하기 위한 좋은 가드입니다. 다만 에러 메시지에 \"데이터는 `data/index.json`을 통해 fetch로 로드해야 합니다\" 같은 **대안 안내**를 추가하면 향후 기여자가 원인을 더 빨리 파악할 수 있습니다.\r\n\r\n---\r\n\r\n## 17. ⚪ [Low] `figureFor()`의 그림 매핑 테이블이 하드코딩되어 데이터 주도(Data-driven)가 아님\r\n\r\n### 위치\r\n`www/script.js` `figureFor(question)`\r\n\r\n```js\r\nfunction figureFor(question) {\r\n  if (question.figure) return question.figure;\r\n  const setId = question.setId || state.setId;\r\n  const sampleCode = String(setId || \"\").split(\"-\").pop();\r\n  const key = `${sampleCode}${question.number}`;\r\n  const figures = {\r\n    A23: \"figures/A23.png\",\r\n    B23: \"figures/B23.png\",\r\n    C23: \"figures/C23.png\",\r\n    C24: \"figures/C24.png\",\r\n    C31: \"figures/C31.png\",\r\n    C32: \"figures/C32.png\",\r\n  };\r\n  return figures[key] || \"\";\r\n}\r\n```\r\n\r\n### 의견\r\n`question.figure` 필드가 이미 존재하여 우선 사용되므로, 이 하드코딩 테이블은 `figure` 필드가 비어 있는 레거시 문제들을 위한 fallback으로 보입니다. `audit-phase3-content.js`에서 그림이 필요한 문제 ID 목록(`imageRequired`)을 관리하고 있으므로, 장기적으로는 모든 문제 JSON에 `figure` 필드를 채워 이 하드코딩 테이블을 제거하는 것이 데이터 일관성 측면에서 바람직합니다.\r\n\r\n---\r\n\r\n## 18. ⚪ [Low] `historyAnswers` 키 필터링 방식 자체의 설계 재검토 권장 (#1과 연계)\r\n\r\n`#1`의 직접적 수정과 별개로, \"현재 세트/모드의 답안만 골라서 히스토리에 저장\"하는 방식 자체가 `state.answers`의 **전체 키를 문자열 패턴 매칭으로 필터링**하는 구조입니다. 이는 `answerKey()`의 포맷이 바뀔 때마다(이번처럼) 조용히 깨질 수 있는 취약한 설계입니다.\r\n\r\n장기적으로는 \"현재 세트/모드에 속한 질문 목록(`currentSet().questions` 또는 `randomQuestions()`)을 순회하며 `answerKey(question, mode)`로 직접 키를 계산\"하는 방식으로 전환하면, 키 포맷이 바뀌어도 히스토리 저장 로직이 깨지지 않습니다 (#1의 제안과 동일).\r\n\r\n---\r\n\r\n## 부록: 파일별 변경 통계 (main 대비)\r\n\r\n| 파일 | 변경 | 비고 |\r\n|---|---|---|\r\n| `www/script.js` | 3333 → 3544줄 (+211) | 데이터 로딩 방식 전환, 오답 노트, 타이머, 그림 키 수정 등 |\r\n| `www/style.css` | 변경 ~30곳 | 설정 패널, note-line, multi-answer-badge, 테이블 등 신규 스타일 |\r\n| `www/index.html` | `<script src=\"./questions.js\">`, `<script src=\"./csts-questions.js\">` 제거 | 신규 fetch 기반 로딩과 일치 |\r\n\r\n---\r\n\r\n## 우선순위 권장 작업 순서\r\n\r\n1. **#1 오답 노트 키 불일치** — 핵심 기능 정상화 (최우선)\r\n2. **#7, #8, #9** — 모바일 UI 클리핑/스크롤 문제 (사용성 직접 영향)\r\n3. **#5** — IndexedDB 복원 경쟁 조건 (재현 어렵지만 영향 큼)\r\n4. **#4** — 랜덤 모드 40문항 가드 (현재는 안전하지만 잠재적 회귀 방지)\r\n5. **#10, #11** — 다크모드/스타일 일관성\r\n6. **#6, #12, #13, #14, #15, #16~18** — 정리성 작업, 여유 있을 때 진행\n\n이거 다시 확인하는데\n전문가들이 사용하는 기준을 사용해서 각 파트별로 검증 기준 20가지씩 세워서 all pass날때까지 수정하고\n\nUI, 데이터 정확성, 문제 가독성 (문제 줄바꿈, 첨부파일 (이미지, 표, 그래프 등등) 제대로 노출되는지) 기획과 일치한지 모두 수정해야해\n</USER_REQUEST>\n<ADDITIONAL_METADATA>\nThe current local time is: 2026-06-14T18:38:38+09:00.\n\nThe user has mentioned some items in the form @[ITEM]. Here is extra information about the items that were mentioned by the user, in the order that they appear:\n\n/goal is a [Slash Command]:\nThe user has marked this task with /goal, indicating that this task is intended to run for a long time without user input, e.g. overnight. You should be extra thorough and only stop when you are confident the goal has been completely fulfilled. The system will force you to continue execution, prompting you to audit your work until completion.\n</ADDITIONAL_METADATA>"

